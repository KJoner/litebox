# LiteBox 项目约束

本文件是 LiteBox 的强制开发约束。所有条款都必须遵守。
技术依据见 `docs/开发计划/v1/Phase0-技术验证报告.md`,
V1 计划见 `docs/开发计划/v1/LiteBox-Panel-V1-开发计划-v2.md`,
V2 计划见 `docs/开发计划/v1_1/v2阶段开发计划.md`。

## 项目定位

面向个人使用的轻量级 sing-box 管理面板。最多 10 个用户,多台 Linux VPS,
节点可低至 128MB 内存。节点落地协议支持 VLESS + REALITY + XTLS Vision
与 Shadowsocks 2022(V4 起)。
V2 增加了普通用户门户与三档节点访问等级,V4 增加了外部代理(机场订阅并入同一条
订阅),V5 增加了配置文件订阅(管理员上传整份客户端配置,面板按用户替换占位符),
V6 增加了一键 TCP 调优(按节点实测到的资源现算内核参数),
V7 增加了节点中转(nginx 透传与 sing-box 链式两种,落地可以是自建节点或外部代理),
V8 让一台机器可以有多个 sing-box 入站(各自的协议、端口、访问等级与出口互不相干),
V9 把外部代理从只收 Shadowsocks 扩展到 VMess / VLESS / Trojan / Hysteria2 / TUIC,
V10 增加了服务巡检与自动恢复、Bark/Telegram 消息推送,以及「节点配置不落盘」,
V11 让 IPv6 条目按入口开关并可单独起名,
V12 把「面板连的地址」与「用户连的地址」拆成两栏(管理 IPv4 / 订阅 IPv4)
并增加了 Clash / mihomo 原生订阅输出,
V13 增加了 Mieru(服务端是 mita,与 sing-box 并列的第二个代理进程),
定位不变。**自建节点的落地协议到 V13 才第一次变长** —— 在此之前
V4~V12 一个字没加。现在是 VLESS + REALITY、Shadowsocks 2022 与 Mieru 三种,
仍然只有这三种:那是我们自己要运维的东西,与登记别人配好的线路是两件事。

## 架构约束

* 不引入 MySQL、PostgreSQL、Redis 或消息队列,只用 SQLite;
* 不引入微服务,保持 Go 单体;
* 不在节点运行常驻自研 Agent。节点上跑的都是上游软件:sing-box、
  nginx(V7 起,只在中转机上)、mita(V13 起,只在有 Mieru 入口的机器上),
  加一个 init 系统(systemd 或 OpenRC)。**面板自己一行代码都不在节点上常驻**;
* 节点不依赖 Docker、数据库或 Node.js;
* 不增加商业机场功能(公开注册、套餐、订单、支付、优惠券、邀请、工单);
  V2 的门户账号由管理员创建,不开放自助注册;
* 不以未来扩展为理由提前引入复杂框架。

## sing-box 相关约束(来自 Phase 0 实测)

以下每一条都对应一个不会报错、不会告警的失效模式,必须严格遵守。

* **不得把 `sing-box check` 当作配置正确性的唯一保证。**
  实测它不校验 UUID 格式(任意字符串会被 `uuid.NewV5` 哈希成合法 UUID),
  也不校验 `flow`(只在连接时校验)。UUID、flow、用户代码格式必须由面板自行强校验。

* **拨测失败时必须把节点上 sing-box 的日志带回来。**
  拨测只看得到"连不通"这个结果,而原因全在节点那一侧的日志里,一行就写清楚了。
  不带上的话,管理员拿到的是「SOCKS5 CONNECT 被拒绝(应答码 1)」——
  那句话准确但没有方向,而真正的原因可能是 REALITY 握手目标解析不了、
  凭据不匹配、或者 flow 写错,三件事的处置完全不同。
  **已经踩过一次**:一台节点的 DNS 挂了(podman 的 aardvark-dns 拒连),
  sing-box 日志里明明白白写着 `REALITY: failed to dial dest: lookup
  www.fastly.com ... connection refused`,而面板只报了「VLESS 链路不通」。
  顺带:**REALITY 服务端对每一个进来的连接都要连一次握手目标**,
  所以节点解析不了那个域名时,拨测与真实用户是同时死的 ——
  「回滚成功、用户未受影响」这句话在这种情况下会骗人,它说的只是
  "这次部署没让事情变得更糟"。日志里的颜色码在带回来之前要擦掉
  (`stripANSI`),它们要进部署记录、推送与浏览器,那几个地方都不认;

* **部署健康检查必须包含真实 VLESS 拨测。**
  不得仅凭 systemd 状态与端口监听判定部署成功。实测 flow 写错时,
  check 通过、systemd active、端口在监听,但全部用户断线。

* **`inbound.users[].name` 与 `experimental.v2ray_api.stats.users`
  必须由同一份用户列表渲染,渲染后断言两个集合相等。**
  实测统计白名单缺项会导致该用户能正常上网但零流量记录,无任何报错。

* **节点重启判定必须使用 `GetSysStats.Uptime`,不得只依赖"计数器变小"。**
  实测漏判会漏算整个重启前的计数值(不是几秒流量)。
  面板自身触发的重启必须在部署事务内显式重置基线。

* **流量同步读取失败必须在进入数据库事务前返回**,
  任何情况下不得把用户流量归零。

* **重启前的强制同步必须在取得节点连接锁之前执行。**
  同步本身也要经 `pool.Do` 读节点 API,而节点级互斥锁不可重入,
  放进部署事务内部会自我死锁。

* **同步失败是否中止部署,取决于节点上的 sing-box 是否正在运行。**
  在运行则必须中止(计数器里有尚未落库的流量,重启会让它永久丢失);
  未运行则跳过并继续 —— 计数器早已随进程消失,此时中止只会让
  崩溃的节点与全新节点永远无法通过部署恢复。

* **REALITY 握手目标必须经过实测校验**:TLS 1.3 + X25519 + 每个 TLS 记录 ≤ 8192 字节。
  检测必须通过 SSH 在节点本机执行(CDN 按地域下发不同证书链)。

* **节点 sing-box 构建标签固定为** `with_utls,with_v2ray_api,badlinkname,tfogo_checklinkname0`,
  不使用上游默认标签。版本固定,禁止使用 `latest`。

* **SSH 连接必须按节点复用长连接**,不得每次同步重新建连(建连约 1.3s,单次调用约 157ms)。

* V2Ray Stats gRPC 的服务名是 `v2ray.core.app.stats.command.StatsService`,
  与 sing-box 自带 stub 的路径不一致,必须用 `conn.Invoke` 显式指定方法名。

## 访问等级与门户约束(V2)

* **有效节点的规则只写在数据库视图 `user_effective_nodes` 里**(迁移 0007)。
  配置生成、订阅、用户门户、额度检查与部署脏标记全部查它,谁都不再自己拼
  等级条件。四处各写一份判断迟早会分叉,而分叉的表现是用户在订阅里看得到、
  连上去却没有凭据,全链路不报任何错。规则若要变更,必须新增迁移
  DROP 后重建视图,不得改动 0007;

* **用户变更的脏标记一律用 `User.EffectiveNodeIDs` 而不是 `NodeIDs`。**
  后者只是 `user_nodes` 里的额外授权;改访问等级不动 `user_nodes`,
  按它标脏会一个节点都标不到 —— 数据库改了而节点全没改;

* **删除用户时的受影响节点必须在打 `deleted_at` 之前取,而且取有效节点。**
  打上标记后视图里就查不到这个用户了,而纯靠等级继承拿到节点的用户
  在 `user_nodes` 里本来就一行都没有;

* **节点等级变更必须自动标脏重新部署**,不像端口变更那样等管理员挑时机。
  等级调高后,被移出的用户凭据还留在节点上,拖多久就多能用多久 ——
  那是权限没有真正收回;

* **编辑节点时 `access_tier_id` 为 0、`subscription_enabled` 为 nil 表示保持原值**,
  不回落到零值。漏传的后果是静默的:VIP 节点被降成普通组等于给全体用户开门,
  订阅开关被关掉等于把节点从所有人的订阅里摘掉,两者都不报错;

* **`access_tier_id` 没有数据库外键**(SQLite 的 ADD COLUMN 在 foreign_keys
  打开时要求带外键的新列默认值必须是 NULL)。`access.Store.Validate` 是唯一的
  拦截点,创建与编辑用户/节点时必须调用 —— 指向不存在的等级会让这一行从
  有效节点视图里整个消失(INNER JOIN),表现为"存在但谁都用不到";

* **订阅与门户只用 `nodes.display_name`,不得出现内部名称。**
  `subscription.Node` 结构体里刻意没有内部名称字段 —— 不是"记得别填",
  而是压根没有位置可填。门户 DTO(`portal.Node` 等)同理,是显式白名单;

* **门户认证与管理员认证是完全独立的两套**:表、Cookie
  (`litebox_portal_session` vs `litebox_session`)、中间件都不共享。
  不做成"同一套认证加个角色字段" —— 那样每加一个接口都要重新判断一次角色,
  判断写漏的后果是普通用户拿到管理权限;

* **门户接口一律从会话取 `proxy_user_id`,不接受任何前端传入的用户标识。**
  改 URL 或请求参数都拿不到别人的数据,因为根本没有一个参数可以改;

* **过期与超额的用户仍要能登录门户。** 挡住他们只会让人连"为什么断了"
  都看不到,只能来问管理员。唯一能挡住登录的是 `login_enabled = 0`
  与账号/用户被删除;

* **强制改密期间除 `/api/portal/auth/me` 与改密、退出接口外一律 403。**
  只靠前端弹窗拦不住直接调接口的人,而这条限制的意义正是在密码改掉之前
  不让初始口令换到任何有价值的东西(尤其是订阅地址);

* **`portal.Store.Upsert` 留空密码表示"不改密码",不是"设为空密码"**;
  改密码则连带撤销该账号的全部会话。停用登录、软删除用户同样立刻踢掉
  在线会话,不等过期;

* **监控数据过期只算 warning,不得把节点判成离线。** 资源采集走的是独立的
  SSH 通道,它失败不代表 sing-box 停了。混为一谈会让管理员在代理完全正常时
  收到"节点离线",几次之后就再也不看预警列表了。从未采样过的节点不报警;

* **仪表盘节点卡片只读数据库里已有的采样,不主动触发 SSH 采集。**
  每次刷新页面都去连一遍全部节点,10 台机器就是 10 条会话,而 128MB 的
  小鸡上这笔开销比它提供的"实时"更值得在意;

* **给已过期的用户续期时,基准取"原到期时间与现在之中较晚的那个"。**
  从原到期日起算的话,给一个过期三个月的人续 30 天,他仍然是过期状态;

* **不限量用户(`quota_bytes = 0`)的"增加流量"必须拒绝。**
  `0 + N` 会把"不限"变成"只有 N",与管理员点这个按钮的意图正好相反;

* `user_adjustments.remark` 是唯一对用户公开的文字,不要往里写内部说明;
  `PublicRecord` 显式列出允许返回的字段,不是把完整记录挑几个字段清空 ——
  后者在结构体加字段时会自动泄漏。

## IPv6 与节点流量额度约束(V2.1)

* **IPv6 只影响订阅内容。** SSH、探测、安装、部署、重启、流量同步与资源采集
  一律走 `nodes.host`。双栈里只留一条管理通道,不然"节点连不上"
  这件事会出现两种互相矛盾的结论,而两条路各报各的错;

* **IPv6 不是第二条 `nodes` 记录**,而是订阅生成时对同一条记录的逻辑展开
  (`subscription.PhysicalNode.Expand`)。拆成两行会带来第二份 config_revision、
  第二串部署记录与第二套资源采样,而机器只有一台;

* **展开必须在读出物理节点之后做,不得在 SQL 里 UNION 造虚拟行。**
  订阅的过滤条件(未删除、未禁用、`subscription_enabled`、已部署过)
  只要写两遍就会分叉,分叉的表现是节点已经进维护、IPv4 条目消失了,
  IPv6 条目却还留在订阅里继续被使用;

* **IPv6 条目的默认名称是 `展示名称-IPV6`**(`subscription.IPv6NameSuffix`),
  固定大写。**后缀本身仍然不做成配置项** —— 客户端靠节点名区分条目,
  全站统一改一次后缀,所有人的客户端里都会同时多出一份重复节点,
  而旧的那份永远留在列表里、永远连得上。单个入口可以覆盖这个名字
  (见下面的「入口级 IPv6 条目」),代价一模一样,只是范围小到管理员能预判 ——
  所以那个动作要走 `lbDangerConfirm` 并把后果逐条列出来;

* **IPv6 公网端口(`nodes.ipv6_proxy_port`)存 0 表示「跟随 IPv4」,
  解析放在订阅生成时(`subscription.PhysicalNode.Expand`),不在写库时固化。**
  写死成当时的 `proxy_port` 之后,管理员再改 IPv4 公网端口,IPv6 条目会继续停在
  旧端口上 —— 而他当初看到的是一个空输入框,不会想到那里存了一个值。
  它只改 IPv6 那一条,IPv4 条目与 `listen_port` 都不动:两条链路指向同一个入站;

* **清空 `ipv6_address` 时必须一并把 `ipv6_proxy_port` 归零。**
  留着它,下次重新填上 IPv6 会静默套用一个几个月前的端口,而那个端口未必还转发着;
  用户拿到的是一条连不上的条目,面板一个错都不报;

* **库里存无方括号的标准化 IPv6**。方括号是 URI 语法的一部分,不是地址的一部分:
  存带括号的值会让 sing-box 客户端配置的 `server` 变成 `"[2602::1]"`,
  客户端解析不出地址,而订阅本身照常下发,看起来一切正常。
  URI 侧由 `VLESSURI` 按需要加括号;

* **`UpdateParams.IPv6Address` 留空表示清空,不是"保持原值"** ——
  与 `AccessTierID`、`SubscriptionEnabled` 的约定相反。清空 IPv6 是管理员的
  显式动作(把 IPv6 条目从订阅撤下来),必须有办法表达;

* **改 IPv6 既不置 `SSHChanged` 也不置 `NeedsDeploy`。**
  前者会白白断掉一条已建立的长连接(重连约 1.3 秒),后者会重启 sing-box
  踢掉全部在线连接 —— 而 IPv6 一个字节都不进节点配置;

* **两个地址栏都收域名(动态 DNS),新建与编辑收一样的东西。**
  原来"新建必须是 IPv4 字面量"是把域名当历史包袱,而换 DDNS 服务商就是要改域名 ——
  拦住它等于逼管理员删了节点重建,而重建会丢掉这台机器的全部历史数据。
  两条路不一致还会出现"这个地址编辑得进去、新建填不进去"的怪事;

* **域名的末段全是数字一律拒掉**(`normalizeHostname`)。放开域名之后最大的风险
  是把**写错的 IP** 当域名收下:`192.0.2`(少一段)、`192.0.2.256`(超范围)
  完全符合域名的字符规则,收下之后面板会拿它去查 DNS,错误信息变成
  「域名解析失败」—— 管理员照着这句话去查 DNS,而真正的问题是地址少打了一段;

* **`host` 填域名时只查 A 记录**(`sshx.ResolveHost`),没有 A 记录直接报错,
  不退化成一个含糊的连接超时。管理通道只走 IPv4 这条不变;

* **`ipv6_address` 填域名时面板一次都不解析它。** 那一栏只进订阅,客户端拿到
  域名自己去查 AAAA。所以在那里填一个只有 A 记录的域名,面板发现不了,
  而用户的客户端会连到 IPv4 上去 —— 界面上要写明这一点;

* **订阅下发域名本身,不下发解析结果。** 这是动态 DNS 的全部意义:IP 变了,
  用户手上那份订阅不用重拉。下发解析结果的话,IP 一变全部用户同时失联,
  而面板这边看起来一切正常(订阅照常生成、节点照常在线);

* **连接池复用连接之前必须重新解析,指向变了就丢掉旧连接**
  (`sshx.Pool.dropIfDomainMoved`)。光靠 `Dial` 时解析不够:一条连接可能活几小时,
  而 DDNS 的 IP 说变就变。不管的话面板会抱着一条通往旧地址的 TCP 连接不放 ——
  旧地址上要么没人应答(操作卡到超时),要么**已经是别人的机器**,
  那时主机密钥校验会当场失败,而管理员看到的是「可能存在中间人攻击」。
  判据是"连着的 IP 还在不在解析结果里"而不是"是不是第一个":一个域名挂多条
  A 记录时解析顺序本来就会轮转,按第一个比会让面板每隔几十秒白白重建一次连接;

* **解析失败时不动连接。** DNS 抖一下就掐掉一条还能用的连接,是拿一个小概率
  故障换一个必然的故障 —— 而此时手上这条连接大概率仍然通着;

* **节点周期边界只有 `traffic.CalculateNodePeriod` 一处实现。**
  节点列表、节点详情、仪表盘预警与前端全部取它算出的结果。各写一遍的话,
  列表说"还剩 3 天重置"、详情说"还剩 2 天",两边都不报错,只能靠猜;

* **重置日超出当月天数时落到当月最后一天,不顺延到下月 1 日。**
  顺延会让二月比一月短一天、三月长一天,长期看每年少算一个周期;

* **节点周期用量只能来自 `traffic_ledger` 按时间范围现算**,
  不得用 `proxy_users.used_*` 反推,也不得拿 `traffic_daily` 当周期边界的依据 ——
  每日汇总表达不了"每月 15 日 UTC 00:00"这种非零点的边界。
  周期切换不删除任何历史数据,改回去也能还原;

* **节点额度的计费口径由 `nodes.traffic_billing_mode` 决定,折算只在读取时做。**
  sing-box 计的是客户端↔节点这一段,而一次用户下载在网卡上要走两趟
  (从源站收一份、再发给客户端一份)—— 按进出合计计费的机器,商家看到的
  约是 sing-box 计数的两倍。`traffic_quota_bytes` 按主机口径填(商家账单上的
  数字),`buildNodeCycleUsage` 一处完成折算:`proxy_bytes` 始终是原始计数,
  `used_bytes` 是折算后的量,`remaining/percent/warning/exceeded` 全部基于后者。
  **分子分母口径不一致是这类统计里最容易出、也最难看出来的错**;
  让上层各自去乘倍数的话,列表、详情、仪表盘预警三处迟早漏一个,
  而漏乘的表现是"额度还剩很多",直到收到超额账单那天才被发现;

* **不做成一律 ×2,认不出的口径一律回落到 ×1。** 两种计费方式都常见,
  甚至同一家不同套餐都不一样。一律翻倍会让按出站计费的机器高报一倍 ——
  额度还剩一半就报红,而管理员分不出这是口径问题还是真超了。
  宁可少报也不能凭一个坏值把所有数字凭空翻倍;

* **计费口径不影响 `traffic_ledger`、`proxy_users.used_*` 与用户额度。**
  用户额度算的是用户自己用了多少,与主机怎么计费无关 —— 给用户额度翻倍
  等于凭空多扣他一半;

* **节点额度只预警,不做任何自动处置**:不停 sing-box、不禁用节点、
  不关 `subscription_enabled`、不删用户凭据。同步有间隔,各家 VPS 的计量口径
  也不同,自动关掉一个共享节点会同时打断这台机器上的全部用户;

* **不限量节点(`traffic_quota_bytes = 0`)的 `remaining_bytes` 与 `usage_percent`
  必须是 null,不能给 0。** 前端拿到 0 会画成"剩余 0 字节"的红色进度条,
  与"不限量"正好相反;

* **告警阈值用整数乘法比较(`used*100 >= quota*80`),不比浮点百分比。**
  恰好 80% 时浮点可能算出 79.99999999999999,边界上该报的警不报;

* **周期时间一律按 UTC 渲染**(`formatUTCTime` / `formatUTCDay`)。
  用本地时区显示会让 UTC+8 的管理员看到"9 月 15 日 08:00 重置",
  而重置日填 31 时本地会显示成下个月 1 日,看起来像设置根本没生效;

* **节点列表的周期流量走批量接口 `GET /api/traffic/nodes-cycle`。**
  逐行查的话 10 台机器就是 10 次扫 `traffic_ledger`,而那是全站写入量最大的表。

## 节点落地协议约束(V4)

V4 起节点可以跑 `VLESS + REALITY` 或 `Shadowsocks 2022`。
实测结论见 `docs/开发计划/v4/V4-技术验证报告.md`。

* ~~**协议是节点级的,一个节点一个入站。**~~ **这一条已被 V8 推翻**,
  见下面的「sing-box 多入站约束(V8)」。当时的理由是「多入站会让流量统计的
  归属变成一个需要重新设计的问题」,而那个问题的答案是根本不需要重新设计:
  V2Ray 的用户计数器里没有入站维度。协议现在是**入站级**的;

* **订阅与门户只反映 `nodes.deployed_protocol` / `deployed_ss_method`,
  不反映 `protocol` / `ss_method` 的期望值。** 改协议到部署成功之间存在一个窗口
  —— 可能二十秒,可能是管理员改完就去忙别的的两小时,也可能是部署失败
  自动回滚之后的**永远**。按期望值渲染的话,这个窗口里用户拉到 `ss://`
  而节点上跑的还是 VLESS,客户端握手失败,而数据库、节点、面板三方都是
  「对的」,只有订阅站在中间说了假话。这两列只由 `MarkDeployed` 写入;

* **`serverPSK:userPSK` 的拼接只有 `singbox.SSClientPassword` 一处实现**,
  拨测与订阅都调它。两处各拼一遍的话,某天改了编码方式只改到一处,
  表现是「拨测通过但用户连不上」,或者反过来 —— 两条路径各自看起来都完全正确;

* **Shadowsocks 节点部署前必须检查节点时钟(部署事务第零步)。**
  SS2022 的 AEAD 头带时间戳,偏差超过约 30 秒时全部用户连不上,而
  `check` 通过、服务 active、端口监听、**本机拨测也通过**。
  拨测测不出来的原因是结构性的:探测客户端跑在节点自己身上,与服务端
  共用同一个时钟,差值恒为零。**已实测**:时钟偏 75 秒时本机拨测照常跑完
  2.36MB 流量。这一步放在事务最前面,中止时节点上什么都还没动过;

* **Shadowsocks 入站不得渲染 `tls` 字段**(`Inbound.TLS` 是指针 + `omitempty`)。
  sing-box 对无关字段是宽容的,不会报错 —— 正因为不报错,一个 shadowsocks
  入站里挂着 TLS 空壳会让排查的人先怀疑配置串了;

* **入站 tag 随协议变**(`vless-in` / `ss-in`),且必须与
  `stats.inbounds` 取自同一处并由 `AssertStatsConsistent` 断言。
  存量 VLESS 节点的 tag 一个字不变 —— `compat_test.go` 钉着
  「升级后渲染结果与 V4 之前逐字节相同」,那是升级不触发全站重新部署的前提;

* **切换到 VLESS_REALITY 前必须已完成握手目标实测**
  (`reality_dest` 非空且 `handshake_checked_at` 不为 NULL)。
  不在切协议时顺带跑检测:那会让「切协议」变成一个可能中途失败的复合操作,
  失败时节点停在半成品状态,而管理员看到的只是一句「检测失败」,
  不知道协议到底切没切。新建 SS 节点时 `reality_dest` 留空,不填默认候选 ——
  显示一个从没实测过的域名,看起来像是这一步已经做过了;

* **改协议与改加密方法置 `NeedsDeploy` 但不自动部署。** 与访问等级变更不同:
  那一条是安全问题(被移出的用户凭据还留在节点上),协议变更是可用性问题 ——
  立刻部署会让全部在线用户在管理员没准备好时断线,而部署完成前订阅仍下发
  旧协议条目,没有人会拿到连不上的东西;

* **两套用户凭据各管一种协议,重置时只标脏跑对应协议的节点**
  (`user.Store.NodesForUserWithProtocol`)。UUID 根本不出现在 Shadowsocks
  节点的配置里,而**部署协调器不跳过无差异部署** —— 一并标脏会把那些机器
  白白重启一遍,把上面全部在线连接踢掉,换不来任何配置变化。
  V4 之前所有节点都是 VLESS,「全部有效节点」恰好是正确答案;现在不是了;

* **用户与节点的 Shadowsocks PSK 固定存 32 字节标准 base64,
  按 method 截取发生在渲染与订阅生成时**(`singbox.SSKeyFor`)。
  按 method 长度生成的话,改加密方法就要重新签发所有人的凭据。
  用**标准** base64 而不是 base64url:sing-box 按标准编码解析 PSK,
  换一种编码会让服务一启动就失败;

* **存量行的 PSK 由服务启动时的一次性 backfill 补齐**,不在迁移里做
  (主密钥在 Go 侧)、也不在配置渲染路径上懒补(`desiredConfig` 也被只读的
  `ConfigDiff` 调用,在「看一眼差异」里写库会把纯查询变成数据变更);

* **自建节点只支持 SS2022 三种方法,不支持传统 AEAD**(`aes-128-gcm` 等):
  后者的多用户没有 EIH,服务端要逐个用户试解密,也没有 replay 防护;

* **`UsersForNode` 一并带出两套凭据,不按节点协议分叉,也不校验 SS 密钥。**
  VLESS 节点上用户的 SS 密钥可以是空的(存量用户等着 backfill),
  拿它拦住渲染会让一个与 Shadowsocks 完全无关的部署做不下去;

* **`ss://` 固定用 SIP002**:`ss://<base64url-nopad(method:password)>@host:port#name`。
  password 的两段本身是标准 base64,带着 `+ / =` 直接进 URI 会被当成分隔符;
  IPv6 只在 URI 里加方括号,sing-box 出站的 `server` 字段不加 —— 与
  `nodes.ipv6_address` 存无方括号是同一条道理;

* **订阅生成分两阶段**:各来源先转成与协议无关的 `subscription.Entry`,
  再按格式输出。三种格式各自认识每种协议的话,加一个协议要改三处,
  而漏掉其中一处的表现是用 A 客户端的用户能连、用 B 客户端的连不上,
  两个人都会以为是自己的客户端有问题。`Entry.Outbound` 是
  `func(tag string) any` 而不是 map:map 按键名字母序序列化,
  会把用户已经导入到客户端里的那份配置的字段顺序整个打乱;

* **单个节点转不出订阅条目时跳过并记错误日志,不让整份订阅失败。**
  订阅失败会让客户端把已有节点**全部清空**,而问题可能只出在一个刚加进来的
  节点上 —— 与 `ErrNotServiceable` 不静默返回空列表是同一条道理。

## 外部代理约束(V4,协议在 V9 扩展)

外部代理是「不属于本面板、不由本面板部署的成品线路」(机场订阅或朋友给的链接)。
它与自建节点只有「能被用户连」这一点相同。

* **协议不再只有 Shadowsocks(V9)。** 收 `SHADOWSOCKS` / `VMESS` / `VLESS` /
  `TROJAN` / `HYSTERIA2` / `TUIC`。但**「能进订阅」与「能当落地」是两件事**,
  由 `Protocol.DialableByNode()` 与 `RelayableByNginx()` 分别回答,
  界限不在协议身上而在**我们这一侧**:节点的 sing-box 是精简构建
  (不含 `with_quic`,见 `scripts/build-singbox.sh` —— 完整标签 58MB / 实占 27MB,
  对 128MB 的机器差距明显),拨不动 Hysteria2 与 TUIC;nginx stream 只渲染
  TCP 的 server 块,而那两种是纯 UDP。用户自己的客户端是完整构建,
  所以它们照常进订阅、照常能用。**在选出口/选转发落地时就把它们滤掉并说明
  少了几条**,不要等到部署:那时错误出现在十几秒后的部署记录里,
  写着一句 sing-box 的 "QUIC is not included in this build",
  而管理员不会想到那是节点二进制的构建选项;

* **协议翻译只有 `externalproxy.SingBoxOutbound` 一处实现**,订阅的 sing-box
  格式、入站的链式出口、中转部署的拨测客户端三个调用方共用。各写一遍的话,
  加一个协议要改三处,而漏掉其中一处的表现分别是:用 sing-box 的用户连不上、
  部署渲染失败、或者**拨测用一份与真实配置不一样的参数去测** ——
  最后一种最坏,它会给一份错的配置发绿灯;

* **Shadowsocks 之外的协议必须带原始分享链接**(`Create` 与 `ReplaceEndpoint`
  都拦)。面板不去拼这类链接:VMess 的 base64(JSON) 与 VLESS/Trojan 的查询串
  各家写法都不一样,自己拼一条丢掉的正是没解析的那些参数,而丢掉之后
  用户能连上、网页能开,只有某些场景不通 —— 与「URI 格式优先透传 `raw_uri`」
  是同一条道理,这里只是把它变成了硬约束;

* **`Params` 一个结构体装全部协议,不按协议拆形状。** 这些字段大量重叠
  (几乎每种都有 TLS 那一组,ws/grpc 传输层在 VMess、VLESS、Trojan 上完全一样),
  拆开要在 JSON 里多一层类型判别,而**旧记录里没有那一层**:
  加字段是向后兼容的,换形状不是。比较用 `Params.Equal`(含切片,不能用 `==`),
  且空 ALPN 与 nil 必须归一化 —— 不然同一条线路每轮同步都被判成「变了」;

* **支持的协议解析失败要报原文,不能归进「按类型跳过」。**
  后者报出来是「跳过 3 条 VMess」,管理员会读成"这个面板不支持 VMess"然后
  不再管它,而真正的原因可能只是那三条链接的 base64 被截断了。
  按类型报数只留给**压根不收**的种类(`ssr://` 之类);

* **`ss://` 的加密方法有两张表,而客户端那张只能有一份。**
  `singbox.ParseSSMethod` 答的是「我们愿意在自己的机器上跑哪几种」(只有 SS2022),
  `singbox.ParseOutboundSSMethod` 答的是「sing-box 能连别人的哪几种」
  (连传统 AEAD 一起)。曾经 externalproxy 自己抄了一份客户端的表,比渲染器那张宽:
  于是 `chacha20-ietf-poly1305` 的机场线路登记得进来、连通性检查是绿的、
  订阅也正常,唯独被设成某个入站的链式出口时,渲染期拿【入站】那张表去校验,
  部署在十几秒后失败并回滚 —— 报错出现在另一个页面上,而管理员刚刚才看到
  这条线路是绿的。`externalproxy/ssmethod_test.go` 钉着「门口收下的每一种,
  渲染器都必须拨得出去」;

* **绝不复用 `nodes` 表**,用独立的 `external_proxies`。`nodes` 的几乎每一列
  都假设「我们有 SSH 和 root」;混进去之后每一处查询都要判断「这行是不是真节点」,
  而判断写漏的表现是面板试图 SSH 到一个机场的服务器上去部署 ——
  那是往别人家机器上发命令;

* **`user_effective_external_proxies` 是独立视图,不与 `user_effective_nodes` 合并。**
  ID 空间会撞(`nodes.id = 3` 与 `external_proxies.id = 3` 是两个东西),
  而后者还被**配置生成与部署脏标记**消费 —— 混进去会让协调器对不存在的节点
  发起 SSH 连接。规则变更同样必须新增迁移 DROP 后重建,不得改 0015;

* **外部代理的任何数字都不进 `traffic_ledger` / `traffic_daily` /
  `proxy_users.used_*`,不影响任何用户的额度判断。** 上游给的
  `Subscription-Userinfo` 是「整个机场账号的总量」,按我们的用户拆不开。
  掺进去的表现是用户在门户看到自己「这个月用了 500 GB」,而那是全站的量。
  它只写进 `proxy_sources.upstream_*`,只在外部代理页展示;

* **门户里外部代理与自建节点分成两个数组返回,不混在一起。**
  混着的话外部代理那几行的流量字段只能填 0,而 0 与「真的没用过」
  长得一模一样。门户 DTO(`portal.ExternalNode`)是显式白名单,
  里面**没有**地址、端口、协议参数,也**没有来源(哪个机场)** ——
  用户知道来源没有用处,只会引出「那我能不能自己去买」;

* **同步失败时一条都不改。** 与「流量同步读取失败必须在进入事务前返回」
  同理:拿不到数据时什么都不做,比按空数据去改状态安全得多;

* **上游消失的条目连续 3 轮才退出订阅,且永不自动删除。**
  机场订阅接口抽风返回部分列表是常事(限流、CDN 缓存、后端故障),
  一次抽风就抹掉用户订阅里的节点、下次同步又回来 —— 用户看到的是
  节点忽有忽无,而这个现象无法复现、无法排查。删掉则丢了管理员配的
  展示名、等级、排序与备注,机场恢复后要全部重配;

* **重新出现时 `missing_rounds` 归零,但 `subscription_enabled` 不自动恢复。**
  连续三轮消失通常意味着那个节点真的下线了,管理员多半已经据此调整了别的东西,
  自动恢复会让订阅在他不知情时变化;

* **`identity_key = sha256(protocol|server|port)`,不含密码。**
  含密码的话机场轮换密码会被判成「旧的消失 + 新的出现」,
  管理员配的展示名、等级、排序全丢。二级匹配键是 `(source_id, raw_name)`,
  救的是「机场换了域名」那种情况;

* **`server` / `port` / 凭据在 IMPORTED 条目上只读,且不可锁定。**
  锁住上游的事实等于故意保留一个连不上的地址。要手工改先「转为手工条目」
  (`source_id` 置 NULL、`origin` 改 MANUAL),那之后不再被同步碰;

* **清空展示名覆盖值 = 「跟随上游」,那时必须解锁而不是再锁一次。**
  不这么处理的话,「改回跟随上游」这个动作本身会把字段重新锁上,
  管理员怎么点都回不到跟随状态,而界面上看不出为什么;

* **源前缀渲染时拼,不写进条目**;而且**前缀不 trim 首尾空格**
  (`CleanPrefix` 而不是 `CleanName`)—— 想要「[家庭] 香港01」的人
  自己在前缀里带一个空格,trim 掉之后他怎么填都会得到「[家庭]香港01」;

* **导入预览里公告条目默认不勾选,但必须全部列出。** 机场订阅前几条
  常是伪装成节点的公告(「剩余流量:100.5 GB」),全量导入会让每个用户的
  客户端里多出几条永远连不上的「节点」。识别规则一定会误伤,
  所以只影响默认勾选状态。**未勾选的条目仍然入库为 EXCLUDED** ——
  不入库的话下次同步它们会作为「新增」再进来一遍;

* **不支持的协议与格式必须按类型报数,不静默丢弃。** 导入 50 条只进来 12 条
  而面板一声不吭,管理员会以为这个机场就只有 12 个节点。格式不支持时要说
  「识别到 Clash / mihomo YAML,本版本暂不支持」,而不是「解析失败」——
  后者会让他以为是地址填错了;

* **URI 与 base64 格式优先原样透传 `raw_uri`,只替换 `#name`。**
  按解析出的字段重新生成会悄悄丢掉不认识的参数(`udp-over-tcp`、`plugin`
  的私有选项、各家扩展),而丢掉之后用户能连上、网页能开,只有 UDP 不通 ——
  没有人会往「订阅生成时丢了一个参数」上想;

* **源到期、被禁用或被删除时,它下面全部条目一起退出订阅。**
  机场账号到期后那些节点就是连不上的,留在订阅里只会让用户以为是自己的问题。
  源的到期取「手工填的优先,没有才用上游给的」(`Source.EffectiveExpiry`)——
  有些机场的 `expire` 头填得不准,让它无声覆盖管理员填的日期,
  会在到期那天出现「面板说还有 20 天」;

* **删除代理源时条目的去向必须由管理员显式选,没有默认值。**
  默认删除会让手滑一次丢掉几十条配置,默认保留会留下一堆无主条目;

* **外部代理的凭据是别人家的账号**:`params_encrypted`、`raw_uri_encrypted`、
  `proxy_sources.url_encrypted` 一律主密钥加密,结构体上打 `json:"-"`,
  查看走单独接口并写审计,**审计详情里绝不出现凭据本身** —— 与节点 root 口令同级;

* **上游名称入库时必须清洗**(换行、控制字符、长度 64)。名字里塞一个换行
  会把 URI 列表的行数搞乱,客户端解析出一个残缺条目;

* **连通性检查只做 DNS + TCP 可达,手工触发,不定时跑**,且结果旁边必须写明
  「这是从面板所在服务器测的」。绿灯不代表用户能连,红灯也不代表连不上;
  定时跑出一堆红灯只会训练管理员忽略这个列表。

## 配置文件订阅约束(V5)

管理员把自己调好的整份客户端配置放进面板(`subscription_profiles`),
用户按客户端类型各拉各的那一份,面板只替换里面的占位符。
与「节点订阅」是两件事:那边给的是一串节点,这边给的是一整份带分流规则、
DNS 与入站的配置。

* **系统里不预置任何模板。** 内置一份就等于承诺维护它的分流规则、规则集地址
  与语法版本 —— 而这些每隔几个月就会变(规则集仓库改名、CDN 被墙、mihomo 改语法),
  坏掉的表现是用户的客户端起不来,而用户会以为是面板坏了。
  管理员没配的类型,门户订阅页上那一整块不出现:不显示灰掉的按钮,
  也不显示「暂未配置」—— 用户对此做不了任何事;

* **三类客户端共用一套替换引擎,不为任何一类写分支。** 小火箭的配置与节点订阅
  是两条独立链接,配置里没有任何跟订阅有关的东西,所以它 1:1 下发 ——
  那是「模板里没有占位符」的自然结果,不是特例代码。写成分支的话,
  那条分支永远不会被第二个场景验证到;

* **Clash 模板必须含 `$(clash_sub_url)` 或 `$(sub_url)`,这是安全要求不是便利性要求。**
  管理员的模板是从他自己在用的配置改来的,里面 `proxy-providers` 的 url 原本是
  **他自己的订阅地址**。忘了换成占位符就保存,等于把自己的订阅链接发给全部用户,
  而且没有事后补救 —— 链接一旦发出去就该按泄露处理;

* **sing-box 模板必须含 `$(singbox_outbounds)`。** 不含它的模板要么里面写死着
  示例节点,要么一个节点都没有。示例配置正是这样(节点定义硬编码、只有分组用了
  占位符),直接上传就以为配好了是这一版最容易犯的错;

* **未知或写错的占位符一律拦下来,不静默保留。** 原样留在输出里的话 sing-box
  只回一句「decode config at line N」,而管理员看到的是用户转述的这句话。
  正文里真需要字面量时用 `$$(` 转义;

* **注释里的占位符不展开**(`blankComments`,三种格式各自的注释规则)。
  管理员写一句「// 这里放 $(singbox_outbounds)」是很自然的事,而展开之后
  几十行 JSON 会被塞进一个 `//` 注释里 —— 只有第一行还是注释,剩下的全是语法垃圾。
  判定必须**宁可漏判不可误判**:误把正文当注释,那里的占位符会原样留在输出里;

* **落地关键词(`落地` / `landing`)写死在代码里,不做成可配置项。**
  Clash 模板里的 `filter: "(?i)落地"` 是管理员自己写的正则,面板管不到;
  两边只能靠同一句约定对齐。做成设置项之后这句约定就变成「去设置页看一眼」,
  而设置页与他正在编辑的模板不会被同时想起来;

* **tag 只由 `subscription.AssignTags` 分配一次。** 出站里的 tag 与三个 tag 列表
  必须来自同一份结果,内置的 `?format=sing-box` 也走它 —— 各算一遍的话,
  重名节点的去重后缀(`香港-2`)可能落到不同对象上,表现是 sing-box 报
  `outbound not found`,而管理员看模板、看节点列表都看不出问题;

* **tag 就是展示名本身**(`sanitizeTag` 只去控制字符)。管理员的模板里除了三个
  占位符之外还会手写分组(比如「专线组」),而他手边只有面板上显示的名字;
  改写过的名字还会让同一条线路在 sing-box 与 Clash 里显示成两个不同的节点;

* **tag 列表展开为空时整份配置渲染失败,不兜底。** 空的 selector 会让 sing-box
  拒绝启动;塞一个 `direct` 进去则是**静默的错误路由** —— 用户以为走的是住宅 IP,
  实际是本机出口。门户上这一份显示不可用并说明「你的可用节点里没有落地节点」,
  用户去问管理员,而管理员看到这句话就知道该做什么;

* **门户展示前每一份都真的渲染一遍**(`Service.ProfileLinks`),只取成功与否。
  轻量检查(数一下落地节点)会与真正的渲染分叉,而分叉的表现是门户写着「可用」、
  点进去拿到一句错误 —— 那比不显示更糟,用户会以为是自己的客户端坏了;

* **落地节点的 detour 目标是模板级字段,保存时校验模板正文里出现过它。**
  指向一个不存在的 tag,sing-box 直接启动失败;而改了分组名忘了改这一栏
  是必然会发生的事;

* **语法自检只报警告,不拦保存。** sing-box 与 mihomo 的解析器对什么宽容无法穷举
  (注释、尾逗号、各自的扩展),我们的检查一定比它们严格;拦下一份它们本来能接受
  的配置,比漏报一个语法错更糟。注释与尾逗号**涂成等量空格而不是删掉**,
  否则报错行号会往前串,而管理员是照着行号去数行的;

* **`?format=sing-box` 保持内置的极简配置,不改成模板。** 它已经在用户的客户端里了;
  同一个 URL 突然吐出一份带 TUN、带规则集、带 DNS 劫持的配置,是在用户不知情时
  改掉他整台机器的网络行为。模板走 `/sub/{token}/profile/{id}` 这条新链接;

* **内置客户端配置里不再输出 `block` 出站。** 它从 sing-box 1.11 起弃用、1.13 移除,
  而这份配置里从来没有任何规则引用过它 —— 新版客户端会因为这一行直接启动失败,
  旧版少了它没有任何影响。`tagBlock` 仍然占着保留字,不能被某个恰好叫 block 的节点抢走;

* **模板软删除,id 不复用。** 用户手上的旧链接永远不会指向一份新配置 ——
  那会在他毫不知情的情况下换掉整台机器的网络栈行为;

* **模板正文不进审计详情。** 一份配置几万字,写进去之后审计日志再也翻不动了,
  而正文本身在库里。审计只回答「谁在什么时候动了哪一份」;

* **重置订阅地址时配置文件的链接必须一起换**(`handlePortalRegenerateSubToken`
  走同一个组装函数)。只换上面三条的话,用户复制下面那几条会得到一个已经失效的地址。

## 节点 TCP 调优约束(V6)

参考脚本见 `docs/开发计划/v6/1C1G主机TCP调优参考脚本.md`,它是照着一台 1C1G 的
Debian 写死的常量表。**这个功能的全部难点在于:那些常量搬到别的机器上会安静地变成破坏。**

* **每个数字都由节点实测到的内存现算,不存模板。** 参考脚本的
  `rmem_max 64MB` 在 128MB 的小鸡上,几条高 BDP 连接就能把内存吃光,表现是
  sing-box 被 OOM killer 干掉;`tcp_max_tw_buckets 200 万` 每桶约 200 字节,
  满载 400MB 比整台机器还大。缓冲区上限取内存的 1/16 并夹在 [4MB, 64MB] ——
  这个系数在 1GB 机器上恰好落回参考脚本的 64MB,所以它不是一个"更保守的替代方案",
  而是同一条曲线在小机器那一端的取值。`planTune` 是纯函数,数值怎么算出来的
  由 `tuning_test.go` 直接盯住,不用连真机;

* **运行期的值一律直写 `/proc/sys` 并读回验证,不以 `sysctl -p` 的退出码为准。**
  LXC/OpenVZ 里 `/proc/sys` 大半是只读绑定挂载,写不进去时 sysctl 只在 stderr
  抱怨一句,退出码还可能是 0 —— 而管理员看到的是"调优完成"。逐个键直写还能拿到
  各自的 errno:EROFS 说明这是容器,EINVAL 说明内核不认这个值,EPERM 说明权限。
  混成一句"sysctl 失败"就全丢了。**判定依据永远是读回来的那个值**,
  errno 只是补充说明;

* **`/etc/sysctl.d` 文件与运行期是两件事,都要做、都要验。** 只直写 `/proc/sys`
  的话下次重启全部消失;只写文件的话当下一项都不生效。写完还要用节点自己的
  `sysctl -p` 解析一遍那个文件 —— 直写成功证明不了文件在开机时读得懂,
  而文件读不懂的表现是:调优当天一切正常,几个月后一次重启,全部参数悄悄回到默认值,
  没有人会把网络变慢和那次重启联系起来;

* **OpenRC 上必须确认 sysctl 服务在 boot 运行级里**(不在就加进去)。
  systemd 的 `systemd-sysctl.service` 一定会跑,OpenRC 的 sysctl 只是个普通服务 ——
  不在运行级里,这份配置重启后根本不会被读;

* **`fs.file-max`、`somaxconn`、`tcp_max_syn_backlog` 只提高不降低。**
  实测发现:systemd 从 v240 起在 PID 1 里把 `fs.file-max` 顶到 LONG_MAX,
  而按 457MB 内存算出来的是 65536 —— 照写下去就是把一个系统级上限调低三个数量级,
  收不到任何好处,却可能让这台机器上别的服务撞到 EMFILE,而报错出现在哪个服务上
  完全看运气。保持不动的键**也不写进 conf**:面板不接管一个不是它选的值。
  **但这条不能推广到所有上限**:`tcp_max_tw_buckets` 与 `rmem_max` 必须能被调低,
  那正是"参考脚本的常量在小机器上危险"这件事的修复动作本身。区别在于
  TIME_WAIT 桶和 socket 缓冲会被正常流量真的用满,而 file-max、backlog
  只在被打到极限时才占内存;

* **临时端口范围放宽到 10000-65535 时,必须把本机在听的端口写进
  `ip_local_reserved_ports`。** 落在这个区间里的 `listen_port` 会在**重启后**
  被某条出站连接抢走,sing-box 起不来 —— 而这件事只在重启那一刻发生,
  面板上一切正常,查起来完全没有方向。取并集不覆盖:这个键可能已经有
  管理员或别的组件写的值,直接盖掉等于悄悄取消他们的保留,而后果同样只在重启后出现。
  没有端口需要保留时**整项不写**,写空值会把已有的保留清掉;

* **第一次调优前把原值快照到 `/etc/litebox/sysctl-baseline.conf`,之后不再覆盖。**
  第二次采就是把面板自己写进去的值当成"原值",还原就再也回不去了。刻意不放在
  `/opt/litebox` 下:那个目录会被「卸载服务」整个删掉,而 sysctl 的改动不会跟着消失;

* **没有基线就拒绝还原,不用内核默认值去猜。** 也不能只删 conf 文件:
  sysctl 的改动在内核里立刻生效,删文件只影响下一次开机 —— 那台机器会带着调优后的
  参数继续跑几个月,而管理员以为已经还原了。快照收录的判据是"这个键在不在",
  不是"值是不是空串":`ip_local_reserved_ports` 原本就常常是空的,而空正是它的原值;

* **不设置 `net.ipv4.tcp_mem`。** 它是全系统 TCP 内存的硬上限,内核已按物理内存自算,
  写死一个值是让这台机器 OOM 最快的路径;

* **不改 systemd 的 `DefaultLimitNOFILE`。** 那会动到这台机器上每一个服务,
  而管理员点的是「TCP 调优」。面板只对自己装的 sing-box 负责:systemd 单元里
  是 `LimitNOFILE=infinity`,OpenRC 脚本里是 `rc_ulimit="-n 65536"`
  (取 65536 而不是"无限":这个值由 shell 的 ulimit 执行,失败会让整个服务起不来,
  而 65536 远低于 `fs.nr_open` 默认上限,在任何内核上都不会被拒)。
  报告里显示的句柄上限读的是 `/proc/<pid>/limits` —— 服务定义里写了什么只说明
  下次启动会是多少,而机器上跑着的是几个月前启动的那个进程;

* **conf 里的注释必须独占一行。** sysctl 不剥离行尾的 `#`,
  `key = 1 # 因为…` 会把注释一起当成值,而写入失败只在 dmesg 里留一句话。
  每一项前面都带一行"这个数字怎么来的" —— 半年后打开那个文件的人不必回到面板才看得懂;

* **磁盘余量在写任何文件之前查,不足直接中止。** 磁盘满时产生的是一个被截断的
  sysctl 配置,它在下次开机时被加载,从截断处起整份失效,而机器上不会有任何提示。
  除此之外磁盘与 CPU 不参与任何取值:缓冲区大小是内存问题,报告里要明说这一点,
  否则"为什么采集磁盘"没有答案;

* **别的 sysctl 文件里的同名键只报不改。** 那些文件是管理员或发行版的,
  动它们比参数不生效严重得多。但必须说出来,并按**文件名**排序判断谁在后面 ——
  排在面板文件之后的会让这次调优在重启后悄悄失效。`/etc/sysctl.conf`
  在 systemd 与 OpenRC 下都排在 drop-in 之后(Debian 干脆把它软链成 `99-sysctl.conf`),
  一律按"在后面"处理;

* **检查阶段不做 modprobe。** 那会加载一个内核模块,而管理员点的是"看一眼"。
  代价是 bbr 常常显示为暂不可用(它多半只是没加载),报告里必须写明应用时会先尝试
  `modprobe tcp_bbr` —— 把"暂时看不到"写成"这个内核不支持 BBR"会让管理员直接放弃;

* **调优不重启任何服务、不断开任何连接**:拥塞算法与缓冲区只对新建连接生效。
  所以它是 `lbDangerConfirm` 档而不是 `LbNameConfirm` 档 —— 摩擦按"能不能撤回"分,
  而这一项既可撤回、又不打断任何人;

* **面板刻意没做的事要显式列出来。** 不写的话,下一个人会以为是漏了,
  然后把 `tcp_mem` 之类的东西补回去。

## 节点入站的监听选项约束(V6)

TCP 调优改的是内核参数,这一节改的是 sing-box 入站里的两个监听选项
(`tcp_fast_open` / `udp_timeout`)。两者分开:前者不重启服务,后者进配置、必须部署。

* **两个字段都必须 `omitempty`,且默认值就是"不出现"。** 存量节点的 TFO 是关的、
  `mem_total_mb` 是 0(还没探测过),渲染结果必须与升级前逐字节相同 ——
  只要有一项被渲染出来,十几台机器同时被判成「需要部署」,而那次重启换不来任何
  配置变化,只会踢掉全部在线连接。`compat_test.go` 与 `listentuning_test.go` 都盯着;

* **凡是改变配置哈希的字段,都必须同时加进 `singbox.compareNodeAttrs`。**
  配置状态(`ConfigStatus`)按整份配置的哈希算,而「配置比对」按字段白名单算 ——
  只改渲染不改白名单,同一个抽屉里上面写着「待部署」、点开比对却说「配置无变化」,
  管理员只能二选一地相信,而两个都是我们自己给的。
  `TestEveryRenderedChangeShowsUpInDiff` 是给以后加字段的人留的:再加一项忘了改 diff,
  在那里补一行就会立刻失败;

* **TFO 是一个开关同时管两端**(节点入站 + 订阅里下发的 sing-box 出站)。
  拆成两个开关只是提供了一种把它配错的方式:服务端开而客户端不请求什么都不会发生,
  反过来客户端发带数据的 SYN 而服务端不认,白多一次回落;

* **默认关,而且不按机器规格自动开。** TFO 的成败取决于用户到节点这一段路径上的
  中间设备(不少会丢弃带数据的 SYN 或剥掉 TFO 选项),而那条路径面板看不到 ——
  探测是从节点本机做的,与用户走的路无关。此外 TFO cookie 是一个跨连接稳定的标识,
  对一个刻意把流量伪装成正常 TLS 的节点来说是白送的指纹。开不开由管理员按机器决定;

* **`tfo=1` 不进 URI。** 它不在 VLESS / SIP002 的分享链接标准里,是各家客户端
  自己的扩展 —— 认不认要逐个确认,而面板没有办法验证。代价是走 proxy-provider
  拉 URI 列表的 Clash/mihomo 用户拿不到 TFO,这一点要在界面上说清楚,
  而不是加一个我们证明不了有效的参数;

* **订阅只看 `deployed_tcp_fast_open`**,与 `deployed_protocol` 同一条道理。
  改开关到部署成功之间的窗口里,订阅仍按旧值下发 —— 否则客户端会对一个还没开 TFO
  的服务端发带数据的 SYN,而数据库、节点、面板三方都是"对的";

* **改 TFO 置 `NeedsDeploy` 但不自动部署**,与协议变更同档(可用性问题,不是安全问题);

* **`udp_timeout` 按内存分档,而"大内存"与"没探测过"都落到不写。**
  大内存那一档算出来就是 sing-box 自己的默认值(5m):写一个与默认值相同的字段,
  行为一个字节都不变,却会改掉配置哈希 —— 于是全站每台机器都显示「待部署」,
  而部署下去什么也没发生。没探测过则不猜,与 TCP 调优里"读不到内存就中止"同理;

* **`udp_timeout` 用字符串时长(`"2m"`)不用数字。** sing-box 的这个字段在历史版本里
  既当过「秒」的整数、又是现在的 Duration(v1.13 的 `UDPTimeoutCompat` 两种都收),
  数字形式在不同版本下含义不同 —— 写错的表现是超时变成几十纳秒或几十小时,
  而配置照样通过 `sing-box check`;

* **内存取 `nodes.mem_total_mb`(探测写入),不读 `node_metrics` 的最新采样。**
  后者每五分钟变一次、还能整个关掉,配置哈希会跟着抖,「已同步」与「待部署」
  两个状态来回跳。**探测读不到内存时不覆盖已有值** —— 冲成 0 会让 `udp_timeout`
  从配置里消失,节点凭空变成待部署,部署下去又加回来:一次读取抖动换来两次全节点重启;

* **`udp_timeout` 的分档边界只有 `singbox.UDPTimeoutFor` 一处实现**,
  接口把算好的值放进 `nodeView.udp_timeout` 下发,前端不按 `mem_total_mb` 自己推 ——
  与「列表里的周期重置日只渲染后端给的 `next_reset_at`」是同一条规矩。

## 节点中转约束(V7)

中转是「让用户连 A、由 A 把流量送到 B」。两种形态,一台 A 上可以同时有:

* **Nginx 透传**(`node_relays`,0..N 条):A 上跑 nginx stream,把 TCP 字节搬到落地。
  A 不解密、不认证、不计流量,客户端与落地之间的协议完全端到端;
* **sing-box 链式**(`nodes.chain_*`,至多一条):A 是完整的落地节点,
  只是出站从 `direct` 换成指向落地的代理出站。

真机验证结论见 `docs/开发计划/v7/V7-技术验证报告.md`。

* **中转主机复用 `nodes` 而不像外部代理那样独立建表。** 那边独立的理由是
  「我们没有它的 SSH 和 root」,中转主机恰恰相反 —— 它需要 SSH、探测、安装、
  部署、监控、额度、访问等级,全是 `nodes` 已有的能力;

* **`nodes.role` 一经创建不可更改。** 落地改中转等于卸掉 sing-box 并丢掉
  这台机器上全部用户凭据;中转改落地则可能与已有的转发规则抢端口。
  两个方向都属于「删了重建」,而重建会丢掉这台机器的全部历史数据;

* **RELAY 上只放 sing-box 二进制,不装服务。** 那台机器上没有 sing-box 配置,
  装一个开机自启的服务只会得到一个反复崩溃重启的进程,而 `Restart=on-failure`
  与 `supervise-daemon` 会让它一直重试,日志刷满而管理员以为「装好了」。
  二进制要留:部署健康检查必须做真实拨测,而拨测需要一个客户端 ——
  它只在部署的那几秒里跑,内存开销是零;

* **RELAY 上无意义的列(协议、三个端口、REALITY、TFO)一律不渲染、不显示,
  也不填一个看起来合理的默认值。** 详情页显示一个从来没有人监听过的端口,
  会让排查的人以为服务没起来。`protocol` 那一列保持默认值只是因为
  迁移 0014 的 CHECK 不接受空串,而 RELAY 上没有任何代码路径读它;

* **`route.final` 缺失是静默的,这条不变量只能由渲染期保证。** 实测:把
  `route` 段删掉、其余一字不动之后,`sing-box check` 通过、服务启动、
  端口监听、客户端握手成功、网页照开,而流量从**中转主机自己的 IP** 出去了
  —— 链式出站定义在配置里,一次都没被用过,没有任何一层报错。
  部署的三步健康检查也全绿(拨测经 direct 回到本机 sshd,照样吐 SSH 横幅)。
  **已决定不做运行期的出口校验**:唯一可行的证据是读落地的链路计数器,
  而那只对自建节点落地成立 —— 两档强度不同的验证会让「部署成功」这四个字
  有两种含义,界面上却分不出是哪一种。于是
  `Config.Route` 与 `chain-out` 出站在同一个分支产生,`AssertChainRouted`
  在渲染最后一步双向断言,`TestChainImpliesRouteFinal` 是它仅有的安全网 ——
  **删掉那个测试等于把上面那份配置放回可能性里**;
  与此配套,部署报告与界面上必须写明「拨测验证的是链路可用,不验证出口落地」;

* **链路凭据不是 `proxy_users` 里的一行**,而是 `nodes.chain_code`
  (`chain_000001`,独立计数器,不复用)。放进用户表的话,每一处查用户的地方
  都要重新判断一次角色 —— 轻则一个不存在的「用户」出现在管理员列表里,
  **重则额度检查把链路凭据停掉,整条链当场断掉而面板上一切正常**。
  与门户认证「不做成同一套加个角色字段」是同一条道理。
  `addUserTraffic` 是条件 UPDATE,链路代码天然匹配不到任何用户 ——
  这一点是**载重的**,改那句 SQL 前先想清楚;

* **同一批流量在两台机器上各记一份,那不是重复计费,是两张账单。**
  实测 10MB:中转侧 `user_000002` 记 10,027,168,落地侧 `chain_000001`
  记 10,027,192(多出的 24 字节是一跳的协议头),而落地上的真实用户是零。
  按节点看两份都对;**全站合计必须排除 `chain_` 前缀**(`traffic.notChainCode`),
  否则仪表盘上那个数字会凭空多出一截;

* **两种下发必须互不牵连**(`deployment.DirtyKind`)。用户变更只改 sing-box 的
  入站用户列表,**nginx 的转发规则一个字都不变** —— 合成一种的话,每改一个用户
  都会把这台机器上全部中转线路白 reload 一遍。反过来,改一条转发规则不该重启
  sing-box,那会踢掉全部在线连接。同一台机器上两种都脏时只等一次 debounce,
  然后**先 sing-box 后 nginx**:反过来的话,刚 reload 好的转发在几秒后被一次
  重启打断,表现是「改了转发规则,结果所有人断了一下」;

* **链式变更是两台机器的有序复合操作,顺序由面板保证。**
  启用:先部署落地(写入链路凭据)再部署中转主机 —— 反过来的话中转的拨测
  握手被拒、失败并自动回滚,而管理员看到的是「中转节点部署失败」,
  不会想到问题在另一台机器上。解除:顺序相反 —— 先动落地的话,
  中转还在用那份凭据,链路当场断掉,而管理员做的事情是「解除中转」;

* **删除节点前必须先取出全部中转关系**(`ReleaseChainsTargeting` /
  `ChainTargetsToRelease` / `HostIDsTargetingNode`)。打上 `deleted_at`
  之后就查不出来了,遗留的后果:指向它的中转主机配置渲染不出来(状态一律变
  「未知」),而真正在跑的配置还指着一台不存在的机器;或者落地上永远留着
  一份没人用、也没人知道是谁的链路凭据 —— 那是权限没有真正收回;

* **中转条目的可见性 = 规则等级 ∩ 用户在落地上确实有凭据**,只写在数据库视图
  `user_effective_relays` 里(迁移 0018)。透传时客户端出示的是**落地的凭据**,
  少了后半条的表现是用户在订阅里看得见、连上去握手直接被拒 ——
  而且跨了两台机器,排查的人会先去查中转主机。
  **落地的 `subscription_enabled` 刻意不参与**:把落地的订阅开关关掉、
  只让用户经中转访问它,正是这个功能最典型的用法;

* **中转条目的协议参数一律取落地的 `deployed_*`**,与订阅那条约束一字不差。
  转换**复用** `EntryFor` / `EntryForExternal`,只把地址换成中转主机的 ——
  另写一套的话,加一个协议要改两处,而漏掉一处的表现是
  「直连那条能用、走中转那条连不上」,用户会以为是中转机坏了;

* **落地是外部代理时,分享链接只替换 authority,其余字节一个不动**
  (`subscription.replaceAuthority`),与「只替换 `#name`」是同一类操作。
  认不出形状时返回空串让调用方按字段重新生成,**绝不返回原串** ——
  那会把落地的真实地址原样发给用户,而中转的意义之一正是不暴露它;

* **地址藏在 base64 正文里的两种方言必须单独认出来**(V9):
  `vmess://base64(JSON)` 与旧式 `ss://base64(method:pass@host:port)`。
  按通用做法处理的话,整块 base64 会被当成 authority 换掉,产出
  `vmess://中转地址:443` 这种既连不上也不像链接的东西,而订阅照常下发 ——
  用户的客户端里多出一条永远失败的节点。做法是解码 → 只动地址那两个键 →
  重新编码,**不按已知字段重建**:各家塞进去的私有键必须原样留着。
  旧式 ss 那一支在只支持 Shadowsocks 的那一版里就已经是错的;
  此外通用那一支现在会先确认 authority 真的是 `host:port`
  (`looksLikeHostPort`)—— 不检查的话,任何认不出的形状都会被当成地址换掉,
  而那正是这两种方言出问题的方式;

* **换掉 authority 的同时,链接里那个【隐含的】SNI 也跟着变了,必须补回原值**
  (`subscription.pinSNI`,V9)。开了 TLS 而链接里没写 `sni` 时,客户端拿
  连接地址当 SNI —— 经中转之后那成了中转主机的名字,落地直接拒绝握手,
  而同一条线路直连完全正常,两个用户会各执一词。补的是**原来就隐含的那个值**,
  不是在改这条链接的含义。代价是落地的 TLS 名字会出现在用户的配置里:
  这一步不做的话这条线路根本连不上,而「连不上」与「藏住了」是两件事 ——
  真正要藏的连接地址仍然只有中转主机。上游明确写过 `sni` / `peer` /
  `servername` 的链接一个字不动;

* **`listen_port` 与 `public_port` 必须分开**,`public_port` 存 0 表示跟随,
  解析放在订阅生成时 —— 与 `nodes.ipv6_proxy_port` 一模一样的理由。
  nginx 的 `proxy_pass` 目标是落地的**公网端口**而不是它的监听端口:
  写成后者在 NAT 机器上是连不上,在直连机器上碰巧一样 —— **后者更糟**,
  它会一直是对的,直到某天落地换成 NAT 小鸡;

* **`nginx -c` 不读发行版的 `/etc/nginx/modules-enabled/`。** 那条 include
  在发行版的 `nginx.conf` 里,而我们整份换掉了。所以独立实例必须**自己渲染
  `load_module`,用绝对路径**。后果很不好排查:一台机器上系统 nginx 的
  `stream{}` 用得好好的,管理员据此确认「这台机器支持 stream」,
  而我们的实例仍然报一模一样的 `unknown directive "stream"`。
  `-c` 的路径也必须绝对:相对路径按编译期 prefix 解析,报的是
  `open() "/var/lib/nginx/nginx.conf" failed`,与配置内容毫无关系;

* **缺 stream 模块是默认情况,不是边缘情况。** 实测 Debian 12 的
  `apt-get install -y nginx` 只装 `nginx` + `nginx-common`,
  `/usr/lib/nginx/modules/` 目录根本不存在;Alpine 的 `apk add nginx` 同样
  不带 `nginx-mod-stream`。两边报同一句 `unknown directive "stream"`,
  **都没有提到缺哪个包** —— 所以安装步骤必须包含模块包,缺失时报出包名;

* **OpenRC 默认没有 reload。** 必须显式写 `extra_started_commands="reload"`
  并自己发 HUP,且 reload 前先 `nginx -t`。不写就只能 restart,而 restart
  会掐断全部在途连接 —— 那会让「改一条转发规则」从普通确认档升到打字确认档。
  已实测:这样写之后 master PID 不变,老新 worker 并存,
  一个在途的 50MB 下载完整跑完;

* **中转拨测失败时必须把 nginx 的错误日志带回来。**
  这条链路上 nginx 只搬字节,它自己几乎不会出错 —— 拨测读不到数据,
  原因几乎总在落地那一端。而 nginx 恰好把那一端的表现一行行记了下来
  (哪个 upstream、来回各多少字节、对面是 RST 还是超时),那正是判断
  「是中转坏了还是落地坏了」唯一需要的材料。**已经踩过一次**:机场换了
  地址,新旧两个地址都不完成 TLS 握手,而面板只报了「经代理未读到任何
  数据: EOF」;与此同时 nginx 日志里写着 upstream 收到 517 字节后
  `Connection reset by peer`,一眼就能看出该去找机场。
  挑行按 `server: 0.0.0.0:<监听端口>,` 匹配而不是只找端口数字 ——
  一台机器上可以有十条规则,而上游地址里也带端口,只找数字会把别条线路的
  故障扣到这一条头上;

* **SOCKS 应答码不能翻译成一句写死的归因。** `socks5Connect` 同时服务
  三条完全不同的链路(自建入站的 VLESS、自建入站的 Shadowsocks、
  经 nginx 到落地),原来那句写死的「说明 VLESS 链路不通」在后两种上是错的
  —— **一句错误的归因比没有归因更糟**,它会把排查引向另一个方向。
  只如实翻译应答码(`socksReplyMeaning`),原因由各自的调用方补充;

* **中转部署的健康检查照样要做真实拨测**,这是本项目第一条铁律,中转不给它开口子。
  探测客户端跑在中转机本机、连 `127.0.0.1:listen_port`(与真实用户走同一条转发),
  CONNECT 目标是**中转主机自己的公网 SSH**,取自 `nodes.host` / `nodes.ssh_port`。
  **不能问节点自己**:NAT 机上 `$SSH_CONNECTION` 给出的是私网地址与本机端口
  (实测 lax-1 上是 `10.10.3.111 22`,而公网是 `154.31.157.27:58739`)。
  发起方是落地而不是中转机自己,所以不涉及 hairpin NAT。
  **链式节点同样要改这个目标** —— 默认的 `127.0.0.1` 会被送到落地,
  打在**落地自己的** sshd 上,于是拨测碰巧仍然通过,但验证的已经不是那台机器了;

* **拨测有三种结局:成功、失败、结构性地测不了。** 后者(落地上一个用户都没有、
  或落地是走 QUIC 的外部代理)记 SKIPPED 并**逐条写明原因**,不判失败
  —— 那不是中转机的错,判失败会让一次完全正确的下发被回滚;但也绝不报成功,
  那等于对一份没验证过的配置说验证过了;

* **落地被删掉的规则跳过,落地未部署过的规则照常渲染。**
  前者此刻已经从视图里消失,继续把流量转到一个不存在的落地毫无意义;
  后者只是还没上线 —— 透传只需要地址与公网端口,拿「没部署过」去卡它
  会让一台配好了、只等落地上线的中转机整份配置都下发不了。
  **落地还在但参数解析不出来时整份配置不下发**:跳过一条等于悄悄把它从
  nginx 上撤掉,而用户手上那份订阅里的地址还在;

* **中转主机的流量面板不计,界面上写明「中转主机,面板不计流量」而不是显示 0。**
  nginx 不接 V2Ray API,那台机器上拿不到任何计数。流量同步与节点周期用量
  都跳过 `role = 'RELAY'`(整行不返回,不是返回一行 0)—— 0 与「真的没用过」
  长得一模一样,这是最容易骗到管理员的一种失败;

* **中转机的配置状态是 `NOT_APPLICABLE`,不是 `UNKNOWN` 也不是 `IN_SYNC`。**
  前者的意思是「本该知道但算不出来」,会催着管理员去查;后者是在说一件不成立的事;

* **门户里中转线路单独一组**,既不并进自建节点(那一组每行都有流量数字)
  也不并进外部代理(那是买来的成品,而中转的凭据是我们发的)。
  `portal.RelayNode` 是显式白名单,里面**没有地址、端口、协议参数,
  也没有落地是谁** —— 那是内部拓扑,用户知道了只会引出「那我能不能直接连落地」;

* **摩擦按能不能撤回、打不打断人来分:** 转发规则的增删改只 reload nginx,
  在途连接一条不断 → 普通确认 / `lbDangerConfirm`;启用或解除链式出站会重启
  sing-box 并连带部署另一台机器 → `lbDangerConfirm` 并逐条列出**两台机器上**
  会发生什么;

* **不做多出口分流、不做中转链路的流量统计、不做自动故障切换、不做端口自动避让、
  不为三跳做任何编排。** 端口冲突检测到就拒绝保存 —— 自动挪端口会让用户手上
  那份订阅静默失效。

## sing-box 多入站约束(V8)

一台落地机器可以有多个 sing-box 入站(`node_inbounds`,迁移 0019),
各自的协议、端口、访问等级与出口去向互不相干。**这一版推翻了 V4 的
「一个节点一个入站」**,而当年那条的理由(流量归属要重新设计)不成立:
V2Ray Stats 的用户计数器名是 `user>>>{code}>>>traffic>>>uplink`,
里面没有入站维度 —— 同一个用户在同一台机器上的流量本来就是合并的,
而那正是 `traffic_ledger` 要的口径。

**真正付出的代价只有一个:入站级的【用户】流量永远拿不到。**
「user_000003 在 8443 那个入口用了多少」这个问题答不了,而且不是暂时没做。
界面上必须直说这一点 —— 不写出来的话,下一个人会以为是漏做了。

* **`nodes` 上那十几列整体搬进 `node_inbounds`,原列就此冻结。**
  不做成「nodes 上的是主入站、新表里的是附加入站」:那样每一处查询都要把
  两个来源并起来,而两个来源迟早分叉 —— 分叉的表现是订阅里看得到某个入口、
  节点上却没有它,全链路不报任何错。谁把那些列加回 `nodeColumns`,
  就等于让同一件事有两个来源;

* **入站 tag 存库、一经分配不可更改**(存量行填 `vless-in` / `ss-in`,
  新建的用 `in-<id>`)。按协议现算的话,同机两个 VLESS 入站会撞成同一个 tag,
  而 sing-box 对重名 tag **不报错** —— 后定义的直接覆盖前一个。
  存库还让改协议不再改 tag,入站级计数器因此是连续的;

* **链式从 `route.final` 改成 `route.rules` 按入站分流 + `final: direct`。**
  `AssertChainRouted` 相应加强成【双向逐入站】的断言,而不是数一数条数:
  规则把甲入站指向乙的出站时,两个入站都通、都有网、都不报错,
  只有出口不是管理员配的那个。`TestTwoChainedInboundsGetTheirOwnOutbound`
  与 `TestAssertChainRoutedCatchesBothDirections/规则指向别的入站的出站`
  是它仅有的安全网 —— 删掉那两个测试等于把这份配置放回可能性里;

* **`final` 必须显式写成 `direct`**,不靠 sing-box「没匹配上就用第一个出站」
  的默认行为:那个默认在配置里看不见,而半年后排查的人需要一眼看出
  没被规则命中的入站从哪里出去;

* **没有任何链式入站时 `route` 整段不渲染。** 直连节点的配置因此与 V7 之前
  逐字节相同,`compat_test.go` 与 `TestDirectNodeRendersNoRouteSection` 盯着;

* **访问等级只挂在入站上,机器没有等级**(迁移 0020)。V8 刚做出来时它是
  「在机器等级之上再收一次」的两层交集,实际用下来那一层是多余的:机器本身
  不接受任何连接,入口才接受。两层同时存在还有一个具体的坏处 ——
  管理员在机器上设了 VIP、又想让其中一个入口对所有人开放,做不到,
  而他会以为把入口调成普通组就行,结果那个入口谁都看不见,面板一个字都不说。
  `nodes.access_tier_id` 就此冻结;

* **迁移 0020 必须把机器的等级搬到它的入口上。** 不搬的话,升级后一台原本
  VIP 的机器,它的入口全是普通组(0019 刻意填的默认值),于是那台机器
  **对全体用户敞开** —— 而管理员什么都没做过,也不会有任何报错。
  权限的静默放大是这类改动里最坏的一种失败;

* **`user_nodes` 的额外授权穿透入口等级。** 它是机器级的,意思就是
  「这台机器给他用」,包括它上面全部入口。不穿透的话,管理员显式把一台机器
  授权给某个用户,而那台机器上只有 VIP 入口,授权就凭空作废了;

* **`user_effective_nodes` 现在是入口那一层的投影**(`SELECT DISTINCT`),
  不再是一条独立的规则。两处各写一遍的话,迟早出现「机器在有效列表里、
  而他在上面一个凭据都没有」,那正是脏标记去重启一台与他无关的机器的来源。
  中转角色的机器因此不在里面 —— 它上面没有任何入站;

* **配置生成与订阅一律查 `user_effective_inbounds`,不查节点那一层。**
  只查节点的话,一台机器上的 VIP 入口会把普通用户的凭据也写进去 ——
  那是权限凭空放大,而且不报任何错;

* **中转规则与链式出站的落地都精确到入站**(`node_relays.target_inbound_id`、
  `node_inbounds.chain_target_inbound_id`)。一台机器上有两个入口时,
  「转发到 B」是有歧义的,而歧义的表现是流量进了管理员没打算用的那个入口
  (协议、端口、等级都不同),没有任何一层会报错。`target_kind` 从 `NODE`
  改称 `INBOUND` 也是为此:改名逼着每一处调用点被重新看过一遍,
  沿用旧值的话那些「按节点解析落地」的代码会继续编译通过;

* **部署的端口监听与真实拨测逐入站各做一次。** 只查第一个等于对其余入站
  什么都没验证过 —— 一个 bind 失败的入站会让整个 sing-box 起不来(服务状态
  那一步能抓到),而一个 flow/凭据写错的入站完全不影响别的入口:
  服务在跑、端口在听、别的入口的用户一切正常,只有这个入口的人全部连不上。
  拨测目标也是逐入站的:链式的那个要绕回本机公网 SSH,直连的那个走回环;

* **部署一台带链式入站的机器之前,必须先确认落地的配置已同步**
  (`checkChainTargetsReady`,判据是落地节点的 `ConfigStatus`)。链路凭据由
  `SetChain` 写进库,但要出现在【落地】的 sing-box 用户列表里才算数,
  而那要落地重新部署一次 —— `ApplyChain` 的两阶段编排正是为此,可是
  **单独部署中转这条路绕开了它**。检查放在 `deployer.Deploy` 之前:
  那时节点一个字节都还没动过,拒绝的代价只是一句话。
  **真机上踩过,花了四轮日志才定位**:落地最后一次部署是 08:22,而中转从
  09:04 起反复部署带 `chain-out` 的配置,落地上只有 `chain_000004~000008`,
  没有这条新链路。于是每一层都正确地沉默了 —— TCP 通,SS 出站认为连接
  建立了(Shadowsocks 没有应答码),落地解密失败直接断开,中转侧只看到
  「经代理完成 SSH 认证失败: ssh: handshake failed: EOF」,而落地日志里是
  「shadowsocks: ... invalid request」。**两句话对不上,而报错落在中转的
  部署记录里,看起来完全像是链路不通**;代价还不只是排查,部署已经重启过
  一次服务,回滚又重启一次。判据保守 —— **只有确定同步了才放行**:
  误拦是让管理员先部署落地(本来就该做),漏拦是两次重启加一句指向错误
  方向的报错,两者不对等。`UNKNOWN` 那一档也拦:它的意思是落地的配置
  渲染不出来,放行只是把失败推迟到十几秒后的拨测;

* **拨测有三种结局,SKIPPED 必须写明原因。** 「这个入站上没有用户」
  (比如刚建的 VIP 入口还没人够格)不是故障,但绝不能混进成功里;

* **`MarkDeployed` 要把不在这次配置里的入站的 `deployed_*` 清空。**
  一个被停用或删掉的入站,部署成功之后节点上就没有它了;不清的话它会继续
  留在所有人的订阅里,而客户端连过去无人应答;

* **回滚后的端口检查读【备份文件本身】的入站端口**,不是这次要部署的那份参数。
  两者未必一样:这次改动本来就可能是改端口或加删入站,拿新端口去查旧配置
  会把一次完全成功的回滚报成失败 —— 那是管理员最需要准确信息的时刻,
  而错误的方向恰好是让他以为节点还坏着;

* **加一个入站不得被 diff 报成「用户更换了凭据」。** 用户在新入站上多出一份
  凭据是加入站这件事本身的结果;报成更换的话,每次加入口都会出现一句
  「N 个用户更换了凭据」,而真正的凭据轮换正是靠这句话被看见的。
  `userMap` 只比较两边都有的那些入站(`commonInboundTags`);

* **入口的增删改一律不自动部署**,与协议变更同档(可用性问题)。
  多入站之后这一点更重:一次部署重启整台机器的 sing-box,会把**全部入口**的
  在线连接一起踢掉,而管理员动的只是其中一个。等级变更仍然自动标脏 ——
  那是安全问题;

* **删除入站是软删除,tag 的唯一索引不带 `deleted_at` 过滤。**
  软删除的入站在下一次部署之前仍留在节点上,它的入站级计数器也还在;
  让新入站抢到同一个 tag,两段互不相干的历史会接在一条曲线上;

* **握手目标是入站级的,节点级的 `dest-check` 只检测不写入。**
  同机两个 REALITY 入站完全可以指向不同的目标,而 8192 字节记录上限是
  目标域名的属性、不是机器的属性。写入必须指名道姓地写到某一个入口上
  —— 悄悄挑一个写进去是这类接口最容易出的那种错;

* **API 端口留在节点级。** 一台机器上只有一个 sing-box 进程,也就只有一个
  API 端点,全部入站的统计都从这一个端口读出来。内存(`udp_timeout` 的依据)
  同理:它是机器的属性;

* **IPv6 地址在节点上,IPv6 公网端口在入站上。** 地址是机器的属性,端口是
  入口的属性。清空地址时必须把这台机器上全部入站的 IPv6 端口一并归零 ——
  留着它们,下次重填 IPv6 会静默套用几个月前的端口(V2.1 那条规矩,
  多入站之后跨了两张表,所以更容易漏);

* **两类入口在同一张表里,但类型标记与操作摩擦必须留着。**
  它们回答的是同一个问题(用户连哪个端口、连上之后去哪),分成两张表要在
  两处之间来回看才拼得出这台机器的全貌。但 sing-box 那几行改了要重启、
  会踢掉这台机器上**全部入口**的在线连接,nginx 那几行只 reload、
  在途连接一条不断 —— 操作按钮因此分两行、各带一句后果,
  确认档次也各走各的。合成一种之后,管理员会对「点这一下要不要挑时机」
  失去判断;

* **「部署」与「安装 sing-box」放在入口 Tab 里。** 部署下发的正是那一屏上
  列着的东西,而按钮离它要改的对象越远,就越难判断这一下会影响谁。
  顶栏只留一个跳过去的入口并说明「待部署」——
  两处各放一个的话,管理员会以为它们是两件事。

## 入口级 IPv6 条目约束(V11)

IPv6 从「机器填了地址就无条件给每个入口加一条」变成「入口自己说了算,
名字也能单独起」(迁移 0022:`node_inbounds.ipv6_enabled` /
`ipv6_display_name`)。它仍然**只影响订阅内容**,一个字节都不进节点配置。

* **`ipv6_enabled` 默认 1,`ipv6_display_name` 默认空串。** 默认关会让升级
  完成的那一刻,全部双栈机器的 IPv6 条目从所有人的订阅里同时消失,
  而管理员什么都没做过、面板也不报任何错;

* **`ipv6_display_name` 空串表示「跟随 IPv4 名字 + 后缀」,迁移里不把当前
  算出来的名字固化进去。** 与 `public_port` 存 0 表示跟随一字不差:固化之后
  管理员改了 IPv4 名字,IPv6 条目会继续停在旧名上,而他看到的是一个已经
  有内容的输入框;而且固化之后「改回跟随」再也没有办法表达;

* **编辑时空串 = 「改回跟随」,不是「保持原值」** —— 与 `access_tier_id` 为 0
  表示保持原值的约定相反,而这里必须相反。当成保持原值的话,管理员把输入框
  清空、保存、再打开,名字还在,怎么点都回不到跟随状态,界面上也看不出为什么。
  代价是**每一处编辑都必须回填这一栏**,漏了就是静默清空(`inboundParamsOf`
  与 `InboundFormModal` 各有一处);

* **回落只有 `subscription.IPv6EntryName` 一处实现。** `node.Inbound` 存原始值,
  派生出的 `ipv6_entry_name` 在 `scanInbound` 里算好下发,前端渲染它、
  **不自己拼后缀**。为此 `node` 包 import 了 `subscription` —— 方向看着是反的,
  但反过来更糟:把规则搬进 `node`,`subscription` 就要 import 整个 `node`
  (含 deployment/sshx/nginx),而公开订阅路由刻意不依赖它们;

* **`PhysicalNode.IPv6Enabled` 的零值是「不展开」,构造处必须显式填。**
  生产只有 `Service.nodesFor` 一处,由 `TestNodesForCarriesIPv6SettingsFromDatabase`
  钉住。漏填的表现是 IPv6 条目从所有人的订阅里静默消失,而面板上那个开关
  明明还开着;

* **IPv6 条目名不得与入口名相同。** 订阅里会出现两条完全同名的节点,用户挑不出
  哪条走 IPv6 —— 而它们是同一个入口的两个地址,「一条通、一条不通」正是
  最需要分辨的时候;

* **清空 `nodes.ipv6_address` 时归零全部入站的 `ipv6_public_port`,但
  【不动】名字与开关。** 端口必须归零(下次重填 IPv6 会静默套用一个几个月前、
  未必还转发着的端口),而名字复用是安全的:重新填上 IPv6 之后,用户客户端里
  那条节点的名字不变,反而少一次「多出一份重复节点」;

* **门户的 `supports_ipv6` 必须两个条件都成立**(机器填了地址 **且** 这个入口
  开着)。只看前者的话,管理员关掉某个入口的 IPv6 之后,门户仍然写着「支持
  IPv6」,而用户拉到的订阅里根本没有那一条 —— 他会去查自己的网络;

* **IPv6 条目不是第二个入口,界面上必须写明这一点。** 它和 IPv4 是同一个
  sing-box 入站的两个地址:协议、监听端口、用户凭据与流量统计全部共用,
  只有名字和公网端口能单独设。不写明的话,入口列表里那个「IPv4 + IPv6」
  标签会被读成「这台机器上有两个入口」;

* **中转条目(nginx 透传)不参与这一版**,它仍然是「机器填了 IPv6 就一起展开」
  (`subscription.PhysicalRelay.Expand`)。两类入口在地址族那一列上规则不同,
  但答案是同一个问题,所以合在一列里给。

## 服务巡检、推送与配置不落盘约束(V10)

* **巡检回答的问题与「节点状态」不是同一个。** `nodes.status` 说的是
  「上次探测能不能连上、上次部署成不成功」,巡检说的是「此刻 sing-box / nginx
  还在不在跑」。一台 ONLINE 的机器完全可能跑着一个已经死掉的 sing-box。
  **巡检绝不写 `nodes.status`** —— 那一列只由探测(`SaveProbe`)与部署成功
  (`MarkDeployed`)写入,让第三个来源掺进去,「离线」这个词就会有三种含义;

* **`UNREACHABLE` 与 `STOPPED` 必须严格分开。** 前者是「SSH 都通不了,
  服务是死是活我们并不知道」,后者是「服务定义在、进程确实没跑」。
  混为一谈会让管理员在一次正常重启后收到「sing-box 停了」,几次之后
  就再也不看这个通道了 —— 与「监控数据过期不得判成离线」是同一条道理。
  SSH 不通时**连续两轮才推送**,而服务没跑一轮就推:后者是一个明确的事实,
  前者可能只是机器在重启;

* **只在「服务确实没跑」时才自动恢复,配置有差异永远不自动部署。**
  服务没跑的那一刻没有任何在线连接会被踢掉,所以重启是零代价的;
  而按配置差异自动部署会在管理员没准备好时重启 sing-box,断掉这台机器上
  **全部入口**的在线连接 —— 那与「入口的增删改一律不自动部署」是同一条规矩;

* **恢复分两步:先直接拉起,拉不起来再重新下发。** 直接拉起不动任何文件,
  代价比重新下发小得多(OOM 被杀、手工 stop 过,这一步就够了);
  而配置不在了(走 tmpfs 的机器重启之后)或者坏了,只有重新下发能救;

* **拉起之后必须再问一次服务状态,不以启动命令的退出码为准。**
  systemd 的 restart 对一个配置有问题的服务可能立刻返回 0,而进程在几百毫秒后
  就退出了。只看退出码的话,巡检会报「已恢复」,下一轮又发现它挂着 ——
  管理员看到的是一台每两分钟恢复一次的机器,而它从头到尾就没起来过。
  这与「部署不得只看 systemd 状态」是同一类错误;

* **`systemctl restart` 之前必须 `reset-failed`。** 服务连续快速失败几次之后
  systemd 会把它标成 start-limit-hit,此后 restart 直接返回
  "start request repeated too quickly" 而根本不去尝试 —— 报的却是一个
  与真实原因毫无关系的错误。配置不落盘之后这条路径变成常态:机器重启后
  /run 是空的,sing-box 每 3 秒失败一次,几轮就撞上限制,而巡检正是要在
  这时候把它救回来;

* **恢复失败要退避,且有上限。** 救不回来的机器多半是真的坏了(磁盘满、
  被商家停机)。每两分钟捅它一次换不来任何东西,只会让日志和推送里
  全是同一条消息,而真正需要被看见的下一条故障就淹在里面;

* **`Notify` 永不阻塞、永不返回 error。** 推送走有界队列,满了就丢并记一行日志。
  巡检卡在一个连不上的推送服务上,是把一个通知故障放大成一个可用性故障;

* **推送地址的路径里就是凭据**(Bark 的设备 key、Telegram 的 bot token),
  与节点 root 口令同级:主密钥加密存储、结构体上打 `json:"-"`、不进日志、
  审计里只写开关状态。**错误信息也要脱敏** —— `net/http` 的错误默认带完整 URL,
  而超时、DNS 失败正是最常被截图求助的那一类;**上游的响应正文同样要擦**,
  不少反代出错时会把请求路径原样回显,泄露从别人的响应里绕回来
  (`notify/http.go` 的 `scrub`,`TestSendErrorsNeverLeakTheURL` 钉着);

* **跨主机重定向一律中止。** 跟着一个 302 走到别处,等于把凭据交出去;

* **三个凭据字段用指针:nil = 保持原值,空串 = 清空。** 界面上凭据永远不回显,
  所以「没动那一栏」必须与「我要清空它」分得开 —— 用普通字符串的话,
  管理员改一下分组名就会把推送地址一起清掉,而界面上什么都不会说;

* **事件集合为空表示【全开】,不是全关。** 反过来的话,新加的事件类型会
  默默不推,而管理员根本不知道有这么一种事件存在;

* **「自动恢复成功」这条通知不能省。** 只报警不报恢复的话,人半夜爬起来
  打开面板发现一切正常,下次就不会再爬起来了;

* **只要这一轮真的动手救过,就一定要推,哪怕上一轮没记录过它坏。**
  也是真机上验出来的:节点重启之后巡检在同一轮里发现并修好了它,
  结果一条通知都没有 —— 面板在别人的机器上重启了服务、重新下发了配置,
  而机器主人完全不知道发生过什么。面板重启后的第一轮是同一种情况。
  这三个分支(救过就推、连不上要两轮、恢复失败升级)都不显然,所以
  拆成了纯函数 `decideAnnounce`,由 `watchdog_test.go` 直接盯住 ——
  留在 announce 里只能靠真机验,而真机上很难把三种情况都造出来;

* **测试推送不受事件开关与冷却影响。** 被压下来会让人以为配置没生效,
  然后去改一个本来就是对的配置。而且它**同步**发送并逐渠道返回结果 ——
  走队列的话界面只能显示「已提交」,而管理员想确认的恰恰是配置对不对;

* **部署失败的推送只挂在协调器上,不挂在 `Deploy` 本身。** 管理员手工点的
  部署,结果就在他眼前;而协调器那条路是用户变更、等级调整顺带触发的,
  失败了没有任何人会知道。

关于「配置不落盘」(`nodes.config_in_ram`,迁移 0021):

* **它不是「启动后删配置」。** 那个做法挡不住它想挡的人:`config.json` 是 0600,
  能读它的人本来就是 root,而 root 可以读 `/proc/<pid>/mem`、可以重启服务
  让面板把配置重新推上来。删文件挡得住的只有拿到磁盘的人 —— 快照、镜像、
  商家手里那块退役硬盘 —— 而那恰恰是 tmpfs 挡得更彻底的。
  删配置还会顺带毁掉三件事:配置比对没得比、回滚没得回、服务一旦重启
  (128MB 机器上 OOM 是常态)就再也起不来;

* **备份必须跟着配置一起走。** `/opt/litebox/backup/config-*.json` 里是
  **同一批凭据**。只把主配置搬进内存而把备份留在磁盘上,等于什么都没做,
  而且更糟 —— 管理员会以为已经做了。临时文件(`config.json.tmp`)同理:
  一次中途失败的部署会把整份配置留在原地。`TestConfigInRAMMovesEveryConfigPath`
  钉着「每一处配置路径都在 RuntimeDir 下」;

* **nginx 的配置刻意【不】跟着进内存。** 两个理由缺一不可:它里面只有
  「哪个端口通向哪个地址」,是拓扑不是凭据;而**它在不在磁盘上,是
  「这台机器下发过转发没有」唯一可靠的判据**,而那个判断正是巡检决定
  要不要自动恢复 nginx 的前提。一起搬走的话,机器重启后巡检会把每一台
  配过转发的机器都判成「还没下发过」,于是永远不会去救它们。
  sing-box 二进制同理留在磁盘上:29MB 放进 128MB 机器的内存里是灾难,
  而它里面没有任何秘密;

* **开启前必须实测 `/run` 真的是 tmpfs,不能假设。** 它在 systemd 与 OpenRC 上
  *通常*是,但一部分极简容器镜像里就是个普通目录 —— 那时配置会老老实实
  写到磁盘上,而面板显示「已开启」,管理员据此认为这台机器上没有凭据了。
  这种错误没有任何迹象。用 `stat -f -c %T`(coreutils 与 busybox 都有),
  不用 `findmnt`(属于 util-linux,Alpine 最小镜像里没有);

* **切换失败时,必须把那次失败的部署刚写到【新位置】的配置清掉。**
  真机上验出来的:关掉这一项时部署失败,面板报「已回退到原来的存放位置」,
  而磁盘上留着一份刚写下的完整配置 —— 里面是全部用户的凭据,
  管理员却以为一个字节都没落盘。反方向同理(开启失败时 /run 里留一份),
  只是危害小得多。清理放在回退部署【之后】:那时服务已经按旧布局跑起来,
  新位置那份没有任何人在用;顺序反过来的话,回退部署万一要读它就读不到了。
  `cleanConfigAt` 的参数说的是"清哪一侧"而不是"这次切到了哪一侧" ——
  两个调用点要清的恰好相反,用 enabled 当参数名的话第二处读起来像写反了;

* **切换的顺序不能变,清理必须在最后:**
  实测 /run → 写库 → 重装服务定义 → 部署 → **成功之后**才清旧位置。
  先清后部署的话,部署失败就再也没有可回退的配置,而服务已经停在一个
  指不到配置的状态 —— 那是把一个可撤销的操作变成了一次事故。
  部署失败时服务定义与数据库都要改回去并重新部署:让机器停在半成品状态,
  而管理员只看到一句「部署失败」,他不会知道服务定义已经被改过了;

* **服务定义里的 `-c` 路径必须跟着这台机器的设置走。** 用全局默认布局
  写 unit 的话,开了这一项的机器上 sing-box 会去读一个永远不存在的路径;

* **`ConfigPath` 从字段改成方法是刻意的。** 它的取值依赖 `ConfigInRAM`,
  而字段表达不了这种依赖 —— 留成字段的话,某处忘了跟着一起改,表现是
  配置写进了 A 路径而服务定义指着 B 路径;

* **`WithConfigInRAM` 返回副本,`Deploy` 按节点复制一份 Deployer。**
  就地改全局那份 layout 会让并发的两次部署互相看见对方的取值,
  而那种错误的表现是配置被写到另一台机器该用的路径上;

* **卸载时必须连 `RuntimeDir` 一起删。** 只删 `/opt/litebox` 的话,
  一台"已经不归面板管"的机器上还留着一份完整的凭据,直到下次重启才消失;

* **这一项与自动恢复是配套的,单独开启反而危险。** 机器重启后配置就没了,
  sing-box 起不来,只能靠巡检重新下发。界面上必须把这条代价写出来 ——
  不写的话,管理员会在一次重启之后以为是面板坏了。

## 订阅 IPv4 地址约束(V12)

一台机器的地址从两栏变成三栏(迁移 0023:`nodes.sub_ipv4_address`)。
在此之前 `nodes.host` 一个人干两件事:管理通道与 IPv4 订阅地址。
两者不同的场景其实很常见 —— 前面挂了一层 IP 转发、管理口与业务口是两个 IP、
或者管理 IP 上根本没有开放代理端口 —— 而那时唯一的办法是把 host 改成用户要连的
那个地址,于是面板自己连不上这台机器。

	nodes.host             SSH / 探测 / 安装 / 部署 / 重启 / 流量同步 / 资源采集
	                       【唯一】的管理通道。下面那一栏留空时兼任 IPv4 订阅地址。
	nodes.sub_ipv4_address 只进订阅,空串表示「跟随 host」。
	nodes.ipv6_address     只进订阅,空串表示这台机器没有 IPv6。

* **端口这一侧一个字都没加。** 迁移 0019 已经把三层端口建好了:
  `node_inbounds.listen_port` 是 sing-box 实际监听的,`public_port` 是 IPv4 条目
  在订阅里用的公网端口(存 0 表示跟随 `listen_port`),`ipv6_public_port` 是
  IPv6 条目的(存 0 表示跟随 `public_port`)。**再加一列"订阅 IPv4 端口"就是
  给同一件事造第二个来源**,而两个来源迟早分叉 —— 分叉的表现是订阅里的端口
  与入口页上显示的对不上,全链路不报任何错;

* **回落只有 `subscription.SubscriptionIPv4` 一处实现。** 订阅条目、中转条目、
  链式落地与接口下发的 `subscription_host` 全部调它。各写一遍的话,分叉的表现是
  面板上显示的地址与用户客户端里那一条不一样,而两边都不报错 —— 而排查
  「用户连不上」的人正是照着面板上那个地址去测的;

* **前端渲染后端算好的 `nodeView.subscription_host`,不自己写那个 `||`** ——
  与「`udp_timeout` 由接口下发」「列表里的周期重置日只渲染 `next_reset_at`」
  是同一条规矩;

* **不在写库时固化成当时的 host。** 固化之后管理员再改 host,订阅条目会继续
  停在旧地址上,而他当初看到的是一个空输入框、不会想到那里存了一个值;
  而且固化之后「改回跟随」再也没有办法表达。与 `public_port` 存 0 表示
  跟随监听端口一字不差;

* **编辑时留空 = 「改回跟随管理地址」,不是「保持原值」**,与 `ipv6_address`
  同约定、与 `access_tier_id` 为 0 表示保持原值相反。代价是**每一处编辑都必须
  回填这一栏**(`NodeFormModal` 与测试里的 `nodeUpdateParamsOf` 各有一处),
  漏了就是静默清空 —— 而清空的表现是全部用户的 IPv4 条目改指管理地址,
  那台机器如果管理口上没开代理端口,所有人当场断线;

* **清空它【不】归零各入口的 `public_port`,与清空 IPv6 正好相反。**
  `ipv6_public_port` 只为 IPv6 条目而存在,地址没了它就没有意义;而
  `public_port` 在 NAT 机器上本来就独立于订阅 IP 存在(服务商映射的外部端口
  ≠ 监听端口),跟着归零会把一台正常 NAT 机的订阅端口悄悄改成监听端口 ——
  用户拿到一条连不上的条目,而面板一个错都不报。两条规矩长得像但方向相反,
  `TestClearingSubscriptionIPv4KeepsPublicPort` 与
  `TestClearingIPv6StillZerosIPv6PublicPort` 放在一起盯着,免得被读成漏写;

* **校验规则与 host 那一栏完全相同**(`normalizeSubIPv4` 复用 `normalizeIPv4`):
  同样收 IPv4 字面量与域名(动态 DNS),同样拒掉末段全是数字的写错的 IP。
  两栏收不一样的东西会出现「这个地址填得进管理栏、填不进订阅栏」的怪事;

* **面板一次都不解析这一栏**,与 `ipv6_address` 同类。所以在这里填一个没有
  A 记录的域名,面板发现不了,只有用户连不上 —— 界面上要写明这一点;

* **它既不置 `SSHChanged` 也不置 `NeedsDeploy`,但【要】置 `RelayTargetChanged`。**
  前两者:一个字节都不进节点配置,而管理通道仍然走 host,断掉那条长连接
  只是白白多一次 1.3 秒的重连。后者:中转的 nginx `proxy_pass` 与链式出站
  指向的正是落地的**对外落脚点**,这一栏一填那个落脚点就换了地址 ——
  不传播的话中转机会继续把流量送到旧地址,而管理员刚在落地那一页上看到"已保存";

* **中转与链式的落地地址取「订阅 IPv4 优先、回落 host」**(`chainInboundTarget`),
  与用户直连这个入站时拿到的是同一个地址。固定用管理地址的话,一台
  「管理口上没开代理端口」的落地会连不上,而部署失败的报错落在**中转的**
  部署记录里 —— 看起来完全像是中转坏了。存量节点这一栏为空,回落到 host,
  行为逐字节不变;

* **拨测的 SSH 目标仍然是 `nodes.host` / `nodes.ssh_port`**,不跟着换。
  那一条打的是这台机器的 sshd,是管理面的事实,与用户连哪个地址无关。

## Clash / mihomo 原生订阅约束(V12)

订阅输出从三种变四种,新增 `?format=clash`(以及 V5 模板里的
`$(clash_proxies)` 等四个占位符)。

**加它的理由只有一条,而且它不是便利性:base64/uri 那两种格式是【分享链接的
列表】,而有些协议根本没有通用的分享链接。** Clash 用户原来只能靠模板里的
`proxy-providers` 去拉那份 URI 列表,于是那些协议对他们等于不存在 ——
用户看到的是自己的节点数比别人少,而没有任何一层报错。

* **`Entry.Proxy` 与 `Entry.Outbound` 的取值范围不一样,这一点是载重的。**
  两边支持的协议各有各的缺口,`nil` 判据必须逐格式各判一次。拿其中一个当
  另一个的近似,会让只有一边支持的协议从另一边的订阅里静默消失,
  而面板上那个节点明明还在;

* **两种格式的名字空间独立**(`AssignTags` / `AssignClashNames` 共用
  `assignTags` 但各带各的保留名与可用判据)。合成一个的话,一个只有 sing-box
  支持的协议会在 Clash 那一份里白白占掉一个名字,让后面同名节点的去重后缀
  (`香港-2`)整体挪一位 —— 而那个后缀已经在用户的客户端里了,挪一位等于给他
  凭空多出一份重复节点。**序号取原列表位置**,所以同一个节点在两种格式里
  拿到的名字仍然一致;

* **协议翻译在 `externalproxy` 里与 `SingBoxOutbound` 并排放**
  (`externalproxy.ClashProxy`)。它们是同一份事实的两种写法,必须一起改;
  `TestEveryProtocolSingBoxSpeaksClashSpeaksToo` 遍历全部协议断言两边
  要么都成功、要么都失败 —— 那个测试里的协议列表**刻意写死**,
  从生产代码取列表的话,新协议会自动被跳过,测试永远是绿的;

* **Clash 不能透传 `raw_uri`,只能按字段重建。** URI 那一侧原样转发上游的
  分享链接(连没解析到的私有参数一起),Clash 这一侧做不到 —— 丢参数的风险
  在这一种格式里是真实存在的,而丢掉之后用户能连上、网页能开,只有某些场景不通。
  界面上要写明两条路不等价;

* **翻不出来的宁可让这一条退出这种格式,不猜。** SS 的混淆插件在 sing-box 里
  是一整串 `k=v;k=v`,mihomo 要的是结构化的 `plugin-opts` —— 只翻 simple-obfs
  一种,别的直接报错。猜着翻会产出一条看起来完全正常、连上去却握不了手的 proxy,
  而用户会以为是自己的客户端或者机场的问题;少一条至少是能被发现的;

* **一条 proxy 都渲染不出来时整份配置失败,不兜底**(`ErrNoClashProxies`)。
  空 `proxies` 会让 mihomo 拒绝启动,而塞一个 `DIRECT` 进去是**静默的错误路由**
  —— 用户以为走的是代理,实际是本机出口。与 V5「tag 列表展开为空时整份配置
  渲染失败」是同一条道理;

* **`DIRECT` / `REJECT` 必须占住保留名。** 让某个恰好叫 DIRECT 的节点抢走它,
  规则里的 DIRECT 会指向那个节点,表现是"直连的流量全走了代理",
  而 mihomo 一个字都不会说;

* **几处 mihomo 与 sing-box 拼写不同的字段,写错就是静默忽略**:
  `reality-opts.public-key`(不是 `public_key`)、
  `congestion-controller`(不是 `congestion_control`)、
  url-test 的 `interval` 是**秒的整数**(不是 `"5m"`)、
  Hysteria2 的 `up`/`down` 是**带单位的字符串**(不是 `up_mbps` 整数,
  且上游没给带宽时整项不写 —— 填 `0 Mbps` 会把带宽钉死成零)、
  httpupgrade 在 mihomo 里**不是独立的 network**,而是 `ws-opts` 上的
  `v2ray-http-upgrade` 开关。这些只能一个一个对着上游文档写,
  然后由 `clash_test.go` 把生成的 YAML 反解回来逐键钉住 ——
  比对字符串会把"换了个缩进"也判成失败,而真正要防的是键名;

* **VMess 的 `alterId` 不能 omitempty。** mihomo 缺这一项会拒绝这条 proxy,
  而 0 正是现在所有机场给的值;

* **TFO 进得了 Clash 原生配置,但仍然进不了 URI。** `tfo` 是 mihomo 文档里
  写明的字段,而 `tfo=1` 不在分享链接标准里 —— 所以走 proxy-provider 拉 URI
  列表的用户拿不到 TFO,拉这份原生 YAML 的拿得到。V6 里那条「Clash 用户拿不到
  TFO」的代价,到这一版只剩在 provider 那条路上;

* **`$(clash_proxies)` 让模板不再需要订阅地址,但绝不能把泄露检查一起绕过。**
  原来「Clash 模板必须含订阅占位符」间接挡住了"留着自己订阅地址"的模板;
  这一版由 `checkNoHardcodedProvider` 单独接住,而且更严:模板里出现
  `proxy-providers` 却没用到 `$(sub_url)`/`$(clash_sub_url)` 一律拒绝。
  它排在必填检查【之前】—— 两条都会拦下同一份模板,而先报通用的「缺占位符」
  会让管理员顺手加一个 `$(clash_proxies)` 就过了,而那个写死的地址还在。
  检查在**去掉注释的正文**上做,理由与占位符不在注释里展开一样;

* **YAML 的缩进是语法的一部分**,展开时必须逐行加前缀,不能像 JSON 那样只靠
  一个 prefix 参数。节点名一律经 `yaml.Marshal` 生成而不是拼字符串:
  以 `-` 开头或带冒号、井号的展示名在 YAML 里都有语法含义,直接拼进去
  轻则名字不对(分组引用不到),重则整份配置报一个与节点名毫无关系的错;

* **`SampleContext` 的样例条目必须同时给 `Outbound` 与 `Proxy`。**
  少了后者,含 `$(clash_proxies)` 的模板会在保存时的自检里被判成不可渲染 ——
  而那份模板本身完全正确,管理员会照着那句话去改一个没有问题的地方;

* **内置的这一份与 `?format=sing-box` 定位相同:只有一条 `MATCH` 兜底规则,
  不下发规则集与 GeoIP。** 想要完整分流的人走「配置文件订阅」,
  那是管理员自己调好、自己负责的一份配置。配置开头的说明写在**文件里**
  而不是只写在面板上 —— 用户几个月后打开这份 YAML 时,面板上那句话
  早就不在他眼前了,而他会在这里加自己的规则然后在某次更新后全部丢失。

## Mieru 落地协议约束(V13)

Mieru 是第三类入口(`node_mieru_inbounds`,迁移 0024)。
**服务端是另一个进程(mita),不是 sing-box 的一个入站** —— 凭据靠
`mita apply config` 下发,流量走 Unix socket 上的管理 gRPC。
真机验证结论见 `docs/开发计划/v13/V13-技术验证报告.md`
(该目录在 .gitignore 里,只在本地),两台机器:
101-la(Debian / systemd / 公网)与 lax-1(Alpine / OpenRC / **NAT**)。

**一台机器上可以有多个 Mieru 入口,各自的出口互不相干** —— 这是实测过的:
同一台机器上三个入口,真实客户端量到三个不同的出口 IP(直连本机
23.169.169.110、链到 lax-1 出 154.31.157.27、链到另一个入站出
23.169.169.110)。但做到这一点的路只有一条,见下面「一个入口一个实例」。

* **绝不做成 `node_inbounds` 的一种 protocol。** 那张表的每一行都渲染成
  sing-box 的一个 inbound;混进去之后配置渲染、部署、拨测、流量采集四条路径
  每一处都要先判断「这一行是不是真的 sing-box 入站」,而判断写漏的表现是
  渲染器把它当成 sing-box 入站写进 `config.json`,服务起不来,
  **而报错指向的是别的入口**。数据层三张表,界面上一张列表;

* **端口是一段而不是一个数**,多端口跳跃是这个协议的主要抗封锁特性。
  三层的含义与 `node_inbounds` 那三列一一对应,只是每层都成了一对起止:
  `listen_port_*` / `public_port_*` / `ipv6_public_port_*`,后两层
  两端都为 0 表示跟随上一层。`Start = End` 表示只有一个端口 ——
  订阅那一侧据此二选一:相等时渲染成 mihomo 的 `port`,不等时渲染成
  `port-range`,**两者在 mihomo 里互斥,同时出现整份配置被拒**;

* **`PortRange` 是类型而不是两个裸 int。** 两个 int 传着传着就会被调换顺序,
  而 `End < Start` 是一个**空集合** —— mita 照常启动、一个端口都不听,
  而面板显示"已下发"。格式化只有 `PortRange.String` 一处实现,
  mita 的 `portRange`、mihomo 的 `port-range` 与 `mierus://` 的 `port` 三处共用;

* **订阅段与监听段刻意不要求相等,也不要求包含。** NAT 机上服务商映射的
  外部段与本机监听段完全可以是两个不相干的号码段,而那正是 public 这一层
  存在的理由。加一条"必须相等"的校验会让那种机器一个入口都配不出来。
  数量不等时**只在表单里提醒**,因为那时客户端会在一部分端口上连不通,
  表现像是「这条线路时好时坏」;

* **端口冲突检测统一在 `internal/nodeport` 一处。** 在此之前它已经有两份
  实现(node 与 relay 各一份),而区间的出现让两处都失效:原来那种逐张表
  `listen_port = ?` 的写法查不出「这个端口落在某个 Mieru 段里」。
  三份实现里漏改任何一处,表现都是同一个 —— 其中一个服务 bind 失败、
  整个起不来,而要到部署的健康检查才发现。做法是把单值当成 `Start = End`
  的一段,于是"区间对区间"是唯一需要写对的逻辑。
  **相交判据是 `a.start <= b.end && b.start <= a.end`,不是"端点落在里面"**
  —— 后者漏判【包含】的情形,而那正是"把一个已有段改窄"最容易造出来的形状。
  `node.ErrInboundPortConflict` 与 `relay.ErrPortConflict` 都指向
  `nodeport.ErrConflict`:各留一个哨兵会让 `errors.Is` 在跨包传递时静默失配,
  而失配的表现是 400 变成 500。`Skip` 的**种类与 id 必须一起给** ——
  只按 id 排除的话,编辑 3 号入站会顺带放过 3 号转发规则与 3 号 Mieru 入口;

* **Mieru 是第一个 `Entry.Outbound` 恒为 nil 的自建协议。** sing-box 完全
  不支持它(入站出站都没有),所以 `?format=sing-box` 与
  `$(singbox_outbounds)` 里出不来 mieru 条目。**不给它一个"差不多"的出站**
  (比如 socks):那会让用户拿到一条连得上、但完全没有 mieru 那层伪装的线路,
  比拿不到更坏。V12 的 Clash 原生输出正是为这一类协议准备的;

* **Mieru 不能当落地 —— 两个方向都不能。** nginx 透传那一路两个理由:
  nginx stream 只渲染 TCP 的 server 块,而更根本的是**一条 `proxy_pass`
  只指一个上游端口,而客户端会在整段里跳**;sing-box 链式那一路是
  sing-box 压根没有 mieru 出站。所以 `node_relays.target_kind` 不新增取值,
  选落地时压根不列 Mieru 入口。**但反过来成立**:Mieru 入口自己可以有出口,
  见下面「出口只能借道本机 sing-box」;

* **用户凭据是第三份**(`proxy_users.mieru_password_encrypted`),与 UUID、
  SS 密钥平级、各管一种协议 —— 复用一把的话,重置其中一种会连带作废另外两种。
  用 base64url 不补等号(与 SS 的标准 base64 相反):mieru 的口令是不透明的
  字节串,谁都不解码它,所以可以选一个不含 `+ / =` 的字母表,
  让它直接进 `mierus://` 的 userinfo 与 mihomo 的 password 而不需要转义;

* **用户名取 `user_code`**,与 sing-box 侧的 stats 计数器同名 ——
  那正是「同一个用户在同一台机器上的流量合并到一条 `traffic_ledger` 记录」
  的来源。换一个值就会让这个人的流量记不到他自己头上;

* **`mierus://` 的端口不在 authority 里**,它是查询参数。mieru 的端口是一组
  而不是一个,塞进 `host:port` 那个位置表达不了 —— 照着别的协议的习惯写成
  `mierus://user:pass@host:port` 的话,客户端会把整个 "host:port"
  当成主机名去解析。`profile` 必填且取条目名:固定成 "default" 的话,
  用户导入第二条会覆盖第一条;

* **传输层与多路复用的取值直接用上游的大写写法**,不翻译:同一个字符串
  既是 mita 配置里 `portBindings.protocol` 的值,又是 mihomo 里 `transport`
  的值 —— 换一种拼写就要在两处各翻一次,而漏翻一处的表现是其中一端
  认不出这个值。多路复用默认 LOW(与 mieru 自己一致),**不主动挑一个
  "更好"的档位**:档位越高越省握手,但也越容易在流量特征上聚成一团,
  而那与这个协议存在的理由正好相反;

* **界面上凡是"这台机器有没有入口"的判断都要把 Mieru 算进去。**
  漏掉的表现是一台只有 Mieru 入口的机器显示「无入口」「谁都连不上」,
  而它好好地在服务用户 —— 一句错误的告警会让管理员去修一台本来就没坏的机器。

以下每一条都是真机上量出来或撞出来的,不是照着文档推的。

* **一个 Mieru 入口 = 一个 mita 实例,不是一份配置里的一个 portBinding。**
  实测:mita 的出口(`egress`)是**实例级**的 —— `EgressRule` 只按目的地匹配,
  `PortBinding` 与 `User` 上都没有出口字段。也就是说同一个 mita 进程里的
  两个端口段**不可能**去往不同的落地。想在一台机器上做到「入口甲直连、
  入口乙走 A 落地、入口丙走 B 落地」,唯一的路是三个实例
  (`litebox-mita-<入口id>`,各自的配置、socket 与服务定义)。
  合成一个实例的话,面板可以正常保存、下发也成功,而三个入口的出口
  **全是最后一条 egress 规则说了算** —— 界面上三行写着三个落地,
  实际只有一个,没有任何一层报错;

* **`/var/lib/mita/metrics.pb` 是写死在 mita 里的路径,多实例必须隔离。**
  它不读环境变量、也没有配置项。两个实例共用一个文件的后果实测过:
  它们互相覆盖对方的计数,而两个实例都"正常运行"、`mita status` 都是
  RUNNING —— 表现是用户流量忽大忽小,而这个现象无法复现、无法排查。
  做法是每个实例一个**挂载命名空间**:
  `unshare --mount --propagation private` 之后 `mount --bind` 各自的
  私有目录到 `/var/lib/mita`。因此**安装 Mieru 的第一步是确认机器上有
  `unshare`**(Debian 的 util-linux 与 Alpine 的 busybox 都带,但不能假设)
  —— 没有它就拒绝安装,而不是装完之后让指标悄悄串味;

* **服务定义里必须 `ExecStartPre` 删掉那个 metrics 文件。** mita 的指标
  **跨重启保留**(这一点与 sing-box 相反),而面板的入账模型是
  「计数器随进程归零」。不删的话,基线与计数器的关系在重启后仍然成立、
  一切看起来正常,直到某次面板自己的基线被清掉 —— 那时整份历史累计值
  会被当成一轮新增流量记进去。删掉它就让 mita 的语义与 sing-box 对齐,
  只需要一套入账逻辑;

* **计数器取 gRPC 的 `DownloadBytes` / `UploadBytes`,不取 CLI。**
  实测 `mita get users` 给的是 1 天 / 30 天的**滚动窗口**且四舍五入到 MiB,
  拿它入账会让用户流量既少算又不单调。gRPC 那两个是
  `COUNTER_TIME_SERIES` 的累积字节数,**只认 COUNTER 与
  COUNTER_TIME_SERIES,GAUGE 一律跳过** —— GAUGE 会跌,拿它做增量
  要么算出负数(ledger 有 CHECK,同步每轮失败)、要么被钳成 0(静默少算)。
  两次独立测量(中间重启过实例)传 10,485,760 字节都得到增量 10,485,976,
  协议开销 0.002%,完全可复现;

* **`node_counters` 的基线必须带来源维度**(迁移 0026,主键加 `source`)。
  一台机器上现在有 N+1 个各自独立重启的计数器(一个 sing-box、N 个 mita)。
  合成一行的后果两种、都不报错:加起来存一行时,sing-box 一重启,
  重启判定会清基线,而那几个 mita **并没有重启** —— 它们已经入过账的
  累计值被再计一遍,用户凭空多出一大截;只存一份时,另外几份的增量
  永远算不出来,那些流量静默丢失。取值用 `mieru:<入口id>` 而不是笼统的
  `MIERU`,理由同上 —— 实例之间同样各自重启。迁移必须是
  `INSERT ... SELECT` 全量搬,**一行都不能丢**:丢一行不是"少算一轮",
  是下一次同步把那个计数器的全部累计值当成新增流量记进去;

* **Mieru 流量同步是每个实例一个 `batch_id`,不是整轮一个。**
  `traffic_ledger` 的唯一索引是 `(batch_id, node_id, user_code, direction)`,
  而同一个用户在同一台机器的两个实例上都有流量是常态 ——
  共用一个 batch 时第二条撞唯一索引,**整轮同步失败,而且此后每一轮都失败**。
  真机上撞到过,日志里只有一句 `UNIQUE constraint failed`;
  单实例的测试永远发现不了这件事,所以有
  `TestMieruSyncAcrossInstancesDoesNotCollide` 钉着;

* **出口只能借道本机 sing-box,那一跳是协议限制。** mita 的出口代理
  **只认 SOCKS5**(上游枚举里只有这一个值),拨不出 VLESS 或 Shadowsocks。
  所以链路是三跳:`用户 → mita → 本机 sing-box 的一个回环 socks 入站
  → 落地`。那个 socks 入站**只监听 127.0.0.1**,而且它的出口由
  `route.rules` 按入站指过去 —— 与 V8 的链式入站同一套机制,
  `AssertChainRouted` 现在把 `mieru-egress-` 这一类也认成需要断言的入站。
  界面上必须把这一跳说出来:不说的话,管理员会问「改个 Mieru 出口
  为什么要重启 sing-box」,而那个问题没有答案就只能被读成面板有 bug;

* **链路凭据必须真的出现在落地的用户列表里,那是要额外接一根线的。**
  `MieruChainUsersForInbound` 存在还不够 —— 它必须被合进
  `inboundUsers` 的返回值。真机上漏接过一次,表现与它自己的注释里
  预言的一字不差:落地照常部署成功,而**入口机**的下发记录里是一句
  `ssh: handshake failed: EOF`,看起来完全像是链路不通,
  排查方向指向另一台机器;

* **带出口的入口:本机那一跳由面板替他做完,落地那一台绝不自动部署。**
  两者的区别不是工作量,是**影响谁**:本机那一跳(装 sing-box、下发它的
  配置)没有任何判断余地 —— 出口要经它,缺了就连不上网;而重启落地会断掉
  **另一台机器上全部用户**的连接,那个决定不该由"我点了这个入口的下发"
  顺带做出。所以 `prepareEgressHop` 自动补齐本机(缺 sing-box 就装、
  配置没同步就下发并重启、再确认那个回环端口在听),
  而落地由 `checkMieruChainTargetReady` 拦下来并说清楚去部署哪一台。
  **自动不等于不告知**:每一步都记进部署结果,尤其是"顺带重启了 sing-box"
  这种会踢掉别人连接的事,确认框里也要逐条列出来;

* **出口那一跳要单独验一次,不能只验完整链路。** 合在一起验的话,
  `mita → 本机 sing-box → 落地` 里任何一环断了都表现为同一句
  「SOCKS5 CONNECT 响应读取失败: EOF」——而那与"mita 坏了"长得一模一样。
  `dialEgressHop` 直接连本机那个 socks 端口(不经 mita)走一次完整的
  SSH 认证:它验的恰好是 mita 之外的全部环节(回环入站在不在、
  route 规则有没有指到落地、落地收不收这条链路的凭据)。
  两跳分开,失败时才知道该修哪一半;

* **探测客户端的收尾要用 `mieru stop`,不能 `pkill -f <配置路径>`。**
  `env FOO=bar cmd` 里的 env 会 exec 掉自己,所以那个进程的 cmdline 是
  `.../mieru start` —— 配置路径只在**环境变量**里,而 `pkill -f` 匹配的是
  cmdline。于是那条 pkill 一次都没命中过:**每做一次拨测,节点上就多一个
  常驻的探测客户端**,带着一个真实用户的明文口令,还听着一个 socks 端口。
  生产上翻出来两个。也不能按二进制名杀 —— 同机可能有别的 mieru 进程
  (另一个入口正在拨测),那会杀掉别人的;

* **下发一个带出口的 Mieru 入口之前,要确认的是【两台机器】,不是一台。**
  `checkMieruChainTargetReady` 管落地(链路凭据有没有出现在它的用户列表里),
  `checkMieruEgressReady` 管**本机**(那个回环 socks 入站有没有下发上去)。
  两个哨兵分开,因为要人做的事不一样:去部署那一台 / 去部署这一台。
  **生产上漏的是后者**:一台管理员刻意不装 sing-box 的机器,Mieru 入口
  配了出口,下发时前五步全绿(端口全在监听、`mita status` 是 RUNNING、
  探测客户端也起来了),只有拨测失败 ——「SOCKS5 CONNECT 响应读取失败: EOF」,
  而那与"链路不通"长得一模一样。真正的原因是 mita 拨到了一个没人监听的
  回环端口。**「这台机器上没有 sing-box」这一档也要拦**,而且它与落地那一侧
  正好相反:那边 NOT_APPLICABLE 说明数据本身不对(中转机不该当落地),
  这边说明**还差一步**,而那一步管理员做得到。判据同样保守 ——
  只有确定同步了才放行;

* **有出口的 Mieru 入口意味着这台机器【需要】sing-box**,哪怕它一个
  sing-box 入口都没有 —— 那份配置里有那个回环入站。所以 `hasNoSingBox`
  的第四个条件是「一个带出口的 Mieru 入口都没有」。漏了它,配置状态会报
  「不适用」,把管理员必须做的那一步藏起来,而他会一直等到下发 Mieru 时
  才撞上拨测失败;

* **改出口要下发三样,顺序不能反:落地 → 入口机的 sing-box → 这个 Mieru 入口。**
  只发其中一个的表现:只发 sing-box → mita 还在直连,出口没变而界面说变了;
  只发 Mieru → mita 指向一个还不存在的 socks 端口,这个入口整个不通。
  接口因此返回 `needs_deploy` 与 `needs_singbox_deploy` 两个标记,
  而不是一个笼统的"要重新部署";

* **`mita apply config` 是整体替换,`mita reload` 只对用户变更有效。**
  实测:apply 对列表字段整体替换,所以撤权只要把人从 JSON 里去掉,
  不需要额外的 `mita delete user`;reload 让用户变更立刻对新会话生效
  (旧会话存活,与 nginx reload 同类),但它**不释放旧端口** ——
  改端口段之后新旧两段会同时监听,而 `mita status` 一切正常。
  所以只有纯用户变更走 reload,其余一律 stop + start;

* **不以启动命令的退出码为准,也不以「守护进程活着」为准。**
  `mita status` 有 IDLE 与 RUNNING 两种,前者是"管理接口在,但没在代理"
  —— 那时端口一个都没绑。与「部署不得只看 systemd 状态」是同一类错误;

* **拨测的 CONNECT 目标按「这个入口有没有出口」分,两个方向都会错。**
  直连入口的 mita 就在这台机器上、出口也是它自己 —— 拿这台机器的**公网
  地址**当目标等于让它绕出去再拐回自己,那要服务商支持 **hairpin NAT**,
  而很多 NAT 小鸡不支持。**生产上撞到过**:端口全在监听、`mita status`
  是 RUNNING、探测客户端也起来了,而拨测一律
  「SOCKS5 CONNECT 响应读取失败: EOF」—— 一个健康的入口被判失败并回滚。
  所以直连打 `127.0.0.1` 加 `$SSH_CONNECTION` 给出的本机 sshd 端口。
  反过来,链式入口打 `127.0.0.1` 会被送到落地、打在**落地自己的** sshd 上
  —— 拨测碰巧仍然通过,但验证的已经不是这台机器了(V8 在 sing-box
  那一侧踩过同一个坑),所以链式必须打这台机器的公网 SSH。
  目标写进步骤详情:两种验的是两件不同的事,失败时管理员第一个要知道的
  就是刚才打的是哪一个。`TestMieruDialTargetSplitsByChain` 钉着;

* **0 字节的 `config.pb` 不算备份。** 上一次下发失败留下一个空文件是完全
  可能的(mita 的 `StoreServerConfig` 用 `os.WriteFile`,写到一半失败就是
  个空文件)。把它当成备份的话,回滚会"恢复"一份空配置,然后
  `mita start` 报「server config is empty」—— 而那句话与真正的失败原因
  毫无关系,排查的人会顺着它去查配置怎么没的。**生产上撞到过**:
  一次拨测失败的回滚,报出来的是配置为空。判据用 `[ -s ]`(存在且非空)
  而不是 `[ -f ]`,两处(备份与回滚)都要改;

* **Mieru 的部署健康检查照样做真实拨测**,用节点上的 mieru 客户端连
  `127.0.0.1:<监听段起点>`,
  并在隧道里**完成一次完整的 SSH 公钥认证**(`sshx.AuthOverConn`)——
  理由与 sing-box 那一侧一字不差:读横幅会被 OpenSSH 的
  `PerSourcePenalties` 当成"连上但没认证就断开"而累积惩罚。
  拨测用**真实用户**的凭据:临时造一个只能证明"新账号能连",
  而且那两次 apply 之间机器上多了一个谁都不知道的账号。
  这个入口上一个用户都没有时记 SKIPPED 并写明原因,不判失败;

* **多路复用的配置形状是 `{"level": "..."}` 而不是一个字符串。**
  写成字符串时 mita 照常接受、照常运行,而客户端连不上 ——
  这是用 `mieru explain config` 对着面板生成的链接才看出来的。
  凡是自己拼的客户端参数,都要拿它验一遍;

* **入口的下发按钮在每一行上,不是整台机器一个。** 一个入口一个实例,
  重启一个不影响其他 —— 做成"下发这台机器的 Mieru"会把本来只该断
  一个入口的连接扩大到全部;

* **`user_effective_mieru_inbounds` 不得借道 `user_effective_nodes`**
  (迁移 0028 重建)。后者是 `user_effective_inbounds` 的投影,而那一层
  只读 `node_inbounds` —— **只认 sing-box**。借道的后果是一台
  「只有 Mieru 入口、不装 sing-box」的机器对**所有人**都查不出用户,
  而那正是这个功能最自然的用法。三处同时静默失效:渲染出的 mita 配置
  users 为空(`mita start` 报 `server mux listening failed: no user found`,
  部署失败并回滚,错误里一个字都没提"这台机器没有 sing-box 入口")、
  这个入口不进任何人的订阅、门户里也看不到 —— 而面板上它显示的是
  启用、在订阅里。**生产上撞到了。** 这个视图自己写那两条规则
  (等级继承 + `user_nodes` 整机授权穿透入口等级),
  `TestMieruOnlyNodeStillHasUsers` 钉着;

* **下发一个 Mieru 入口之前必须确认它至少有一个用户**(`ErrMieruNoUsers`)。
  上游只在 `ValidateFullServerConfig` 里放行空用户列表(注释写着
  "不是错误,只是不工作"),真正 bind 端口时却直接拒绝。拦在动节点之前 ——
  那时一个字节都还没改,拒绝的代价只是一句话;走到回滚要重启一次实例,
  而报错落在一句与"没有用户"毫无关系的话上;

* **mita 与 mieru 二进制必须先传临时路径再 rename。** 直接覆盖会得到
  ETXTBSY,而这台机器上每个 Mieru 入口都有一个 mita 进程正在执行那个文件
  —— **只要装过一次,重装就一定失败**。经 SFTP 时那个 errno 变成一句
  `sftp: "Failure" (SSH_FX_FAILURE)`,完全看不出是"文件正在被执行"。
  sing-box 那一侧早就这么做了;

* **加一种下发就要改迁移里那条 `deployments.kind` 的 CHECK。**
  生产上撞到了:V13 加了 MIERU 这一种,而迁移 0018 那条 CHECK 只写了
  SINGBOX 与 RELAY,于是**每一次 Mieru 下发都落不了库** ——
  下发本身照常跑完,只有记录没了,部署记录页上一条都没有,
  管理员只能去翻 journalctl,而那里按设计只写失败步骤名与错误的第一行。
  `TestEveryDeployKindCanBeSaved` 遍历 `Kind` 的全部取值,
  再加一种而忘了改迁移会在那里失败,而不是在生产上失败;

* **`mita start` 失败必须带回 mita 自己的日志与配置文件的大小**
  (`mieruStartError`)。这是「拨测失败必须把节点日志带回来」在 Mieru
  这一侧的落实。生产上遇到过一句
  `ValidateFullServerConfig() failed: server config is empty` ——
  它说的是"守护进程读到的配置是空的",准确但没有方向,而能让它变空的
  原因至少有三种(守护进程带着旧环境在跑、配置文件被别的东西截断、
  apply 打到了另一个实例的 socket 上),分辨它们要的恰好就是那两样。
  **只带回字节数,不带回内容**:那份 .pb 里是全部用户的口令哈希,
  而部署记录还会进推送与浏览器;

* **流量同步、配置状态与端口摘要三处也在这条线上,而且各自的判据不同。**
  同步跳过 sing-box 那一半的判据是 `deployed_config_sha256 != ''`
  (与巡检的 `wantSingBox` 一字不差):有入口但从没下发过的机器上同样没有
  那个进程,而"入口配好了"与"进程在跑"是两件事。不跳的话,那台机器上
  **每一轮**都拿到一句 `connection refused` 并记一条 WARN ——
  日志里每分钟一条,几轮之后管理员就再也不看这个通道了,
  而真正的同步失败就淹在里面。配置状态的判据多一个条件
  (`hasNoSingBox`:没有 sing-box 入口 **且** 从没下发过 **且** 有 Mieru 入口)
  —— 入口被删光但服务还在跑的机器**恰恰需要**下发一次去撤掉它们,
  报「不适用」会把一个真实的待办藏起来,而被移出的用户凭据还留在节点上。
  端口摘要则要把 Mieru 的端口段一起算进去:漏掉的话标题行上写着
  「端口 无(一个入口都没有)」,而用户正连着它的 31000-31004;

* **凡是回答「这台机器上该有什么」的地方,都要问一句"它有几类入口"。**
  探测、巡检、配置比对、概览、入口管理五处的判据原来都只认 sing-box,
  而一台只有 Mieru 入口的机器在每一处都会被判错,方向还各不相同:
  探测把「没装 sing-box」归进 `Problems`,而那一档决定 `Usable()`、
  `Usable()` 直接写 `nodes.status` —— 那台机器显示「离线」,
  而它正靠 mita 服务用户;比对报「节点上尚无配置」,那会被读成"部署丢了",
  而那份 config.json 本来就不该存在;概览说「一个入口都没有,谁都连不上」。
  **归错档比少显示一项坏得多** —— 一句错误的告警会让管理员去修一台
  本来就没坏的机器,几次之后他就再也不看那个状态了。
  探测的口径因此由调用方给(`ProbeParams.WantSingBox`),而不是让
  `Probe` 自己猜:这台机器上有什么,只有调用方知道;

* **巡检的 Mieru 状态必须逐实例给,不能合成一个。**
  一个入口一个 mita 实例,它们各自独立地跑与崩。把"有一个在跑"算成正常,
  挂掉的那个再也不会被发现;合成一句「Mieru 没在跑」,管理员看不出该查
  哪一个 —— 而要去救的也只是其中一个。判据是 systemd active **加**
  `mita status` 是 RUNNING:守护进程活着只代表管理接口可用,IDLE 时
  一个端口都没绑,而服务在 systemd 眼里是 active 的;

* **`deployments.kind` 那条 CHECK 与 `Kind` 的取值必须同步**(迁移 0027)。
  V13 加了 MIERU 而没改迁移,于是**每一次 Mieru 下发都落不了库** ——
  下发本身照常跑完,只有记录没了,部署记录页上一条都没有,
  管理员只能去翻 journalctl,而那里按设计只写失败步骤名与错误的第一行。
  `TestEveryDeployKindCanBeSaved` 遍历全部取值,再加一种而忘了改迁移
  会在那里失败,而不是在生产上失败;

* **`mita start` 失败必须带回 mita 自己的日志与配置文件的大小**
  (`mieruStartError`)。生产上遇到过一句
  `ValidateFullServerConfig() failed: server config is empty` ——
  准确但没有方向,而能让它变空的原因至少有三种。**只带回字节数,
  不带回内容**:那份 .pb 里是全部用户的口令哈希,而部署记录还会进
  推送与浏览器;

* **`user_effective_mieru_inbounds` 不得借道 `user_effective_nodes`**
  (迁移 0028 重建)。后者是 `user_effective_inbounds` 的投影,
  而那一层只读 `node_inbounds` —— **只认 sing-box**。借道的后果是一台
  「只有 Mieru 入口、不装 sing-box」的机器对**所有人**都查不出用户,
  而那正是这个功能最自然的用法。三处同时静默失效:渲染出的 mita 配置
  users 为空(`mita start` 报 `server mux listening failed: no user found`)、
  这个入口不进任何人的订阅、门户里也看不到 —— 而面板上它显示的是
  启用、在订阅里。**生产上撞到了**,`TestMieruOnlyNodeStillHasUsers` 钉着;

* **下发一个 Mieru 入口之前必须确认它至少有一个用户**(`ErrMieruNoUsers`)。
  上游只在 `ValidateFullServerConfig` 里放行空用户列表(注释写着
  "不是错误,只是不工作"),真正 bind 端口时却直接拒绝;

* **mita 与 mieru 二进制必须先传临时路径再 rename。** 直接覆盖会得到
  ETXTBSY,而这台机器上每个 Mieru 入口都有一个 mita 进程正在执行那个文件
  —— **只要装过一次,重装就一定失败**。经 SFTP 时那个 errno 变成一句
  `sftp: "Failure" (SSH_FX_FAILURE)`,完全看不出是"文件正在被执行"。
  sing-box 那一侧早就这么做了;

* **整机卸载要连每个 mita 的服务定义一起删。** 它们在
  `/etc/systemd/system` 下,而 `rm -rf /opt/litebox` 删不到 ——
  留下的是一堆指向不存在文件的服务,每次开机各失败一次,
  而机器上再也没有任何东西能解释它们是哪来的;

* **手动点「同步流量」必须两路都同步**(`Scheduler.SyncNodeNow`)。
  真机上发现过:点完手动同步,Mieru 的 10MB 一个字节都没进去,
  而接口返回的是一次成功 —— 管理员据此看到的用户用量缺了一整截,
  却与真实值长得一模一样,要等下一轮定时同步才悄悄补上。
  **部署事务不走这条路**(它用 `deployment.TrafficSyncer.SyncNode`,
  只同步 sing-box),所以这里加上 Mieru 不会让每次 sing-box 部署
  白连 N 个 socket。sing-box 那一半成功而 Mieru 那一半失败时,
  错误信息里必须说明前半截**已经落库了** —— 否则管理员会以为
  这次点击什么都没发生,而用户用量已经变了。

## 前端约束(V3)

设计规范见 `docs/开发计划/v3/litebox-web-ui/`(该目录在 .gitignore 里,只在本地)。

* **颜色、圆角、间距、阈值只在 `web/src/theme/tokens.ts` 里定义一次。**
  组件里要用颜色就从它 import;scoped CSS 里只允许出现 tokens.ts 已有的那几个值,
  新增颜色必须先加进 tokens.ts。原来 `#cf1322` / `#d46b08` / `#389e0d`
  散在四个文件里各写一遍,改一次要改四处,而且已经开始不一致了;

* **`ant-design-vue 4.2.6` 的 component token 命名停在 AntD v5.0 那一代。**
  设计稿里的 `Table.headerBg`、`Layout.siderBg`、`Menu.itemSelectedBg` 这类键
  在这个版本既过不了类型检查、运行期也被静默忽略 —— 照抄只会得到「配了但没生效」。
  更麻烦的是 Table 与 Card 在自己的 styleFn 里用 `mergeToken` 重算派生 token,
  `components.Table` 传进去的值会被当场盖掉,根本没有 Token 通道;它们改走
  alias token(表头/行 hover 底色走 `colorFillAlter`)或 `styles/antd-tune.css`。
  往 antd-tune.css 里加规则时必须写明「为什么不能走 Token」;

* **「去节点上做事」的结果一律走进度弹窗,不用吐司**
  (`NodeOpProgressModal`)。装、卸、下发、重启都要连 SSH,少则两三秒、
  多则二十几秒,而结果恰恰是最需要读的:失败时要看卡在哪一步,成功时
  要看它到底做了什么(顺带打开了 sshd 的转发?回滚了?拨测跳过了?)——
  三秒吐司交付不了一份步骤表。三条规矩:**跑的时候关不掉**(遮罩、ESC、
  × 全禁用,否则随手一点关掉,结果几秒后才回来、已经没有地方呈现,
  表现为"点了下发,等了一会儿,窗口自己没了");**跑完不自动关**
  (自动关等于把结果藏起来);**失败时也要显示已经做完的步骤**
  ——"停了服务但没删定义"与"什么都没做"要人做的事完全不同;

* **操作跑完之后的重拉要等弹窗关掉。** 页面的 reload 会把 loading 抬起来,
  而那一屏在 loading 期间整个换成骨架 —— 面板连同刚跑完的那份结果一起
  被卸载。真机上看到的正是"点了安装,转了几秒,窗口自己没了",
  而操作其实是成功的;

* **节点详情顶栏那个菜单只放机器级操作**(编辑节点、重新引导、
  重置主机密钥、删除节点)。装/卸/下发是**服务**的事,而一台机器上有
  三类服务(sing-box、每个 Mieru 入口一个 mita、nginx),它们的影响面
  差得很远 —— 放在顶栏里只能写一句放之四海而皆准的确认文案,
  而管理员恰恰要靠那句话判断这一下要不要挑时机。三类的装/卸/下发
  统一在「入口」Tab 里,一行一类,离它们要改的东西最近;

* **「扫描握手目标」在 sing-box 入口的表单里,不在节点的只读检查里。**
  握手目标是**入口级**的:同机两个 REALITY 入站完全可以指向不同目标,
  而 8192 字节的记录上限是那个域名的属性、不是这台机器的属性。
  放在节点那一层的话,扫完还要再挑一次"写到哪个入口上" ——
  而悄悄挑一个写进去正是这类操作最容易出的错。
  **新增与编辑两条路的差别要写在界面上**:新增时选中的域名只是填进表单、
  跟着「新增」一起提交,没有实测;编辑时点一下就立刻写库,与保存按钮无关
  —— 写入必须经 `ApplyHandshakeDest` 实测通过,而切协议要求
  `handshake_checked_at` 不为 NULL,那一列只有它会写;

* **状态一律走 `LbStatusTag`,不裸用 `a-tag`。** 状态是「形状 + 文案 + 颜色」
  三重编码,AntD Tag 只有后两重。打印、投屏、色觉障碍下颜色全部失效,
  而节点的「离线」与「部署失败」都是红的 —— 只靠颜色分不出该修网络还是该重部署;

* **节点的运行状态与配置状态必须分两列。** 一台在跑旧配置、上次部署失败的机器,
  挤在一格里只显示「部署失败」,看不出它其实还在正常服务用户;反过来一台 ONLINE
  的机器也可能跑着三次变更之前的配置。配置状态取后端 `config_state`,
  **前端不推导** —— 在线状态、`config_revision`、最近部署记录三样都答不了
  「库里的配置是否已在节点上生效」,猜出来的「已同步」会让管理员不去部署;

* **趋势图缺失的日子传 `null`,不补 0、不插值。** 后端的每日流量接口只返回
  确实有记录的日子(全站、按节点、按用户、门户四处一致)。补 0 会把
  「同步任务没跑完」画成「当天没人用」,插值会凭空造出一个从未存在的数字;

* **读取失败绝不回退成 0。** 指标卡取不到数时显示「—」并说明原因。
  `summary?.traffic_month ?? 0` 会让页面稳稳地显示「本月流量 0 B」,
  读不到和真的是零长得一模一样,这是最容易骗到管理员的一种失败;

* **列级失败不能升级成整表失败。** 周期流量、资源采样各自 catch,只让那一列
  显示「—」,并在表格上方显式说出来 —— 否则一整列的「—」看起来像所有机器都挂了。
  整表失败时表格保持空白,不显示「暂无数据」,那会被读成「一条都没有」;

* **危险操作按「能不能撤回」分四档,不按听起来吓不吓人。**
  可逆且局限用普通确认;可逆但影响面大(重启、部署、重新生成订阅地址)用
  `lbDangerConfirm` 并逐条列出影响范围;不可逆的只有四个(删除用户、删除节点、
  卸载服务、重置主机密钥)用 `LbNameConfirm`。**给重启这类可逆操作也加打字摩擦,
  管理员很快会变成无脑复制粘贴,真正不可逆的四个反而失去警示作用**;

* **`LbNameConfirm` 要求输入的是节点的内部名称,不是展示名称** ——
  内部名称唯一,展示名称可以重复;

* **窄屏 <768 整表换卡片(`LbRowCard` + `useNarrow`),不靠表格横向滚动。**
  AntD Table 的横向滚动会把最右边的「操作」列推到屏幕外,手机上找不到它。
  桌面的紧凑控件不许直接带到触屏:缩小间距可以,缩小命中区不行;

* **门户与后台是两套界面语言,不是同一套加个角色判断。** 门户里不出现侧栏、
  表格排序、批量选择、技术字段(UUID、hash、rev、sha256、IP、CPU 内存),
  也不出现 HTTP 状态码与请求 ID —— 用户拿它做不了任何事,只会以为自己该看懂。
  门户不显示节点地址与资源占用:露出来只会引来「你的机器内存 96% 了是不是要炸」;

* **抽屉与弹窗里的渲染错误必须被 `onErrorCaptured` 收住并关掉容器。**
  Vue 渲染期抛错会卸载出错组件的子树,而 a-drawer 的遮罩是另一个 DOM 节点,
  会原样留在屏幕上 —— 管理员看到的是「详情页没了、屏幕一片灰」,不知道发生了
  什么,也只能靠点遮罩脱身。兜底里不要 `return false`:错误仍要冒泡到控制台,
  否则下次排查时一条线索都没有;

* **抽屉里有只读检查在跑时,遮罩点击与 ESC 一律失效。** 探测、扫描握手目标要
  几秒到十几秒,期间抽屉外面整片都是遮罩,随手一点就关掉了,而结果几秒后才返回、
  已经没有地方呈现 —— 表现为「点了探测,等了一会儿,详情页自己没了」,
  像是探测把页面搞崩了。右上角的 × 不拦:那是明确的关闭意图,不是误触。
  同时工具条上必须写明正在跑什么,否则关不掉这件事本身又成了新的谜;

* **`Modal.confirm` 的 `onOk` 后面还要开进度弹窗时,不得返回那个 Promise。**
  AntD 只要拿到 Promise 就把确认框留在屏幕上转圈等它 resolve,而部署要 15~25 秒,
  这期间进度弹窗已经打开,两个 Modal 同层叠着,后开的反而被压住。写成
  `onOk: () => { void doDeploy() }` 让确认框先落幕。进度弹窗另给一个高于 1000 的
  z-index —— 确认框收起有淡出动画,同层时会在那两百毫秒里盖住半截;

* **候选握手目标里不放苹果与谷歌系**(`www.apple.com`、`dl.google.com`、
  `gateway.icloud.com`)。它们的 TLS 参数都合格,问题是各类教程都用这几个域名,
  一台 VPS 常年只跟它们握手本身就是特征。`destcandidates_test.go` 钉着这条 ——
  只写在注释里的话迟早会被顺手加回来;

* **节点列表按 `sort_order` 升序,并把排序值显示出来。** 它决定订阅与门户里的
  先后,藏在编辑弹窗里的话管理员改完看不出改到了第几位。前端再排一遍不是多余:
  改完排序值到下一次 `load()` 之间,列表还是旧顺序。`id` 兜底不能省 ——
  全部留 0 时没有兜底就是不稳定排序;

* **列表里的周期重置日只渲染后端给的 `next_reset_at`,不按 `reset_day` 自己推。**
  边界只有 `traffic.CalculateNodePeriod` 一处实现(31 日在二月落到当月最后一天),
  各算各的会让列表说「9 月 1 日」、详情说「8 月 31 日」,两边都不报错;

* **字体自托管,不引 CDN。** 面板要 embed 进单个二进制,内网与断网环境下
  CDN 字体只会静默回落到系统字体栈。中文引 `@fontsource/noto-sans-sc` 的
  **分片版**(`400.css`/`500.css`)而不是整包 `chinese-simplified` ——
  前者带 unicode-range,浏览器只下载页面上真正出现的那几片。
  `vite.config.ts` 里的 `dropWoffFallback` 插件负责剔除 `.woff` 回退:
  两份都打进去会白白多出 6.4MB,而 woff2 从 2016 年起全线支持。

## 工程约束

* 不假设 VLESS 用户可动态热更新,用户变化必须通过配置生成和安全重启生效;
* 所有重启前必须先同步流量,同步失败必须中止部署;
* **一次已经开始的节点操作不得因为客户端断开而中止。** 挂 `longOperation`
  的处理器都在改节点上的东西(部署、安装、重启、切换配置存放位置、
  启用链式出站),它们的 ctx 必须 `context.WithoutCancel` 与请求解绑,
  `Service.Deploy` 的收尾(部署记录、`MarkDeployed`/`MarkDeployFailed`)
  再解绑一次 —— 它还有协调器与巡检两条调用路径,不能靠调用方替它守住。
  **生产上发生过**:一次 `node.chain_apply` 被中途掐断,日志里只剩三行
  `context canceled`(保存部署记录失败、标记节点部署失败状态出错、
  写入审计日志失败),而面板上连一条部署记录都没有,管理员只能去翻
  journalctl。断开的原因通常不在面板里 —— 反向代理的 `proxy_read_timeout`
  默认 60 秒,而部署两台机器本来就要更久。成功那一侧更坏:`MarkDeployed`
  没跑,入站的 `deployed_protocol` 停在旧值,那个入口会从所有人的订阅里
  消失或下发一份与节点上不符的参数,而节点上跑的是新配置 —— 三方各说各话,
  没有任何一层报错。`deployer.rollback` 早就这么做了(回滚半途退出会让
  节点停在坏配置上),这一条只是把同一条道理补到了数据库那一侧;
* **一次部署的结局必须落进系统日志,不能只落部署记录。**
  部署的全部过程只存在于 `deployment.Result.Steps` 里,而它只经
  `deployStore.Save` 落库 —— Save 一失败(数据库锁、磁盘满、ctx 被取消),
  这次部署就再也没有任何痕迹可查。上面那次事故里,journal 里除了三行
  `context canceled` 什么都没有,连「卡在哪一步」都答不出来,而节点上的
  sing-box 确实重启过。`logDeployResult` 因此在 Save **之前**先写一行,
  两条部署路径(sing-box 与 nginx)共用它,消息里分得出是哪一种 ——
  后者只 reload、不打断在途连接,混在一起会让「部署失败」被读成有人断线了。
  日志里**只写失败步骤名与错误的第一行**:拨测失败时错误带着节点上
  sing-box 的日志原文,而那里面可能有用户 UUID,journal 通常谁都读得到,
  部署记录才是有访问控制的地方。回滚结果必须带上 —— 它回答的是
  「节点现在还能不能用」,那与「这次部署失败了」是两个问题;
* **走到回滚的错误必须打上 `sshx.RemoteFailure`。**
  `pool.Do` 在传输层错误上会重连并【重跑整个 fn】,而部署事务整个跑在
  一次 `pool.Do` 里 —— 走到回滚说明配置已经换过、服务已经重启过,
  重跑一遍会再写一次配置、再重启一次服务,把这台机器上全部在线连接
  又踢一次。更糟的是拨测会再打一次,而 PerSourcePenalties 按次数累积:
  第二轮因此比第一轮更容易失败,于是管理员看到的是**第二轮**的失败原因。
  **光靠字符串判不出来** —— 拨测失败的错误几乎一定带 `EOF`(隧道通了、
  对端在 SSH 握手时直接断开),而它与「这条连接坏了」长得一模一样,
  正好命中 `connectionErrorMarkers`。**真机上撞到过**:一次链式部署
  跑了两轮、35.997 秒、服务重启两次。反过来,**写配置之前的失败不打标** ——
  那时节点一个字节都没动过,而 `Pool.ensure` 不探活、长连接可能几小时没用过,
  那次重连正是为它准备的;
* 所有配置替换前必须执行 `sing-box check`;
* 所有部署失败必须自动回滚;
* 不通过字符串拼接生成 JSON,必须用结构体序列化;
* 不通过字符串拼接执行远程 shell,所有远程参数严格校验;
* 所有数据库变更必须使用迁移,不得修改已应用的迁移文件(迁移框架会用哈希拦截);
* 每个阶段完成后必须运行测试并更新文档。

## 代码约定

* 所有时间存 UTC 的 RFC3339 字符串;
* 流量单位统一字节;
* 需要还原的敏感字段(用户 UUID、节点 REALITY 私钥)用主密钥加密存储;
* 订阅 Token 只存 SHA-256 哈希;
* 用户删除优先软删除;
* 用户代码格式 `user_000001`,一经分配不可变更、**不可复用**
  (它是流量统计的唯一标识;复用会让新用户继承旧用户的 ledger 历史。
  因此由 system_settings 中的独立计数器分配,不从现存行推导);
* 用户的所有变更必须经 `user.Service` 而不是直接调 `user.Store`,
  否则受影响节点不会被标脏,数据库改了而节点没改;
* **一台机器只能承载一个节点**:`deployment.Layout` 的路径(`/opt/litebox`)
  与服务名(`litebox-singbox`)是全局固定的,两个节点指向同一主机会互相覆盖配置;
* **入站的两个端口含义不可混用**:`node_inbounds.public_port` 是客户端连接的
  公网端口,只写进订阅;`node_inbounds.listen_port` 是 sing-box 的
  `inbound.listen_port`,端口监听检查与拨测查的也是它。NAT 主机与 nginx 转发时
  两者不同,把公网端口渲染进节点配置会让 sing-box 监听在转发链路另一端的号码上
  —— `check`、systemd、健康检查全部通过,只有用户连不上。
  `singbox.InboundParams` 里只有 `ListenPort`,公网端口不属于节点配置。
  (V8 之前这两列在 `nodes` 上,现已冻结;`public_port` 存 0 表示跟随
  `listen_port`,解析放在订阅生成时);
* 入站的握手目标不走通用的 `Store.UpdateInbound`,必须经 `ApplyHandshakeDest`
  实测通过后写入,否则会绕过 8192 字节记录上限的校验;
* **节点服务的一切操作必须经 `deployment.InitSystem`,不得直接写 `systemctl`**。
  节点可能是 systemd,也可能是 OpenRC(Alpine 是 NAT 小鸡最常见的镜像,
  正落在本项目瞄准的 128MB 区间)。init 系统在部署事务开头探测一次即可,
  不要落库 —— 节点重装换系统后数据库里的值就是错的;
* **OpenRC 脚本必须用 `supervisor="supervise-daemon"`**。默认的
  start-stop-daemon 不会在进程退出后拉起它,而 128MB 机器上 OOM 是常态,
  少了它节点崩一次就再也不会自己恢复(systemd 侧对应 `Restart=on-failure`);
* **节点的 sshd 必须允许 TCP 转发,面板在「安装 sing-box」时自动打开它。**
  流量同步、握手目标实测、部署健康检查的 VLESS 拨测全走 direct-tcpip 通道,
  而不少加固过的镜像把 `AllowTcpForwarding` 关成 no。关掉之后这三样一起失败,
  sshd 只回一句 `administratively prohibited` —— 不提配置项也不提哪台机器,
  而 session 通道不受影响,所以「测试 SSH」与「探测」照常通过,
  看起来只有代理坏了。四条硬规则:
  判断靠**实测**开一条通道(`sshd -T` 的输出与实际行为未必一致);
  写入的那一行必须在文件**最前面**(OpenSSH 取首次出现的值,追加到末尾无效);
  只加不删、不注释已有的行(它们可能在 `Match` 块里,是管理员的刻意限制);
  用 **reload 不用 restart**,且 reload 之后必须再实测一次 ——
  改完配置就宣布成功,与「部署只看 systemd 状态」是同一类错误。
  任何一步失败都恢复原文件:宁可让管理员自己去开,也不能留下一个改坏的
  sshd_config,那台机器下次重启 sshd 就再也连不上了;

* **凡是回答「节点现在允不允许转发」,都必须新开一条连接**
  (`CheckTCPForwardingFresh`)。sshd 在 accept 那一刻就把配置解析进了这条连接的
  子进程,之后 reload 只对新建的连接生效 —— 拿连接池里那条长连接去问,
  **两个方向都会答错**:刚打开转发时它说"仍被拒绝",于是面板把一个完全正确的
  修改回滚掉;节点后来被改成禁止时它说"允许",于是面板跳过修复,等哪天重连
  才突然全线失败。前一种已经在生产上发生过(节点日志里是
  「写入 AllowTcpForwarding yes 并 reload,但 SSH 通道仍然开不起来,已恢复原文件」),
  而这条路径在此之前从没在真正禁用转发的机器上跑过;

* **drop-in 文件名取 `00-` 前缀,与 sysctl.d 正好相反。** OpenSSH 对绝大多数
  关键字取**首次出现**的值,而 drop-in 按文件名顺序加载 —— 排在前面的赢;
  sysctl 那边却是后来者覆盖先来者。两处我们都要写 drop-in,照着另一处的直觉
  取前缀就会写出一个必然失效的文件,而写入、`sshd -t`、reload 全部成功;

* **只确认「有 Include」不够,要确认那条 Include 的通配符真的会读到我们的文件**
  (`includeCovers`)。指向别处的 Include 会让面板往一个没人读的目录里写文件,
  三步全绿而通道照样开不起来;

* **改完 sshd 之后必须丢掉连接池里那条连接**,而且一律丢、不判断"这次有没有
  真的改过" —— 判断本身就要依赖那条不可靠的连接。留着它,紧接着的第一次部署
  会卡在 VLESS 拨测并自动回滚,而管理员刚看到「安装成功」。
  `Invalidate` 必须在 `pool.Do` 之外调用:节点锁不可重入;

* **复测失败时先采诊断再回滚。** 回滚会把我们写的文件删掉,而最有价值的证据
  恰恰是「我们的文件写着 yes,而 `sshd -T` 或实际行为仍是 no」—— 它直接指向
  是谁在覆盖。诊断只读四样:`sshd -T` 的实际生效值、各配置文件里设过这几项的
  行(带文件名与行号)、Match 块、authorized_keys 里公钥那一行前面的
  `restrict` / `no-port-forwarding`。少了文件名与行号,管理员只能挨个翻;

* **`ProbeResult` 的 `Problems` 与 `Warnings` 不可混用。** 前者决定 `Usable()`,
  而 `Usable()` 会把节点状态写成 OFFLINE;TCP 转发被禁属于「能跑,但面板某些
  功能用不了」,归 Warnings。混进 Problems 会让管理员在代理完全正常时
  收到「节点离线」—— 与「监控数据过期不得判离线」是同一条道理;

* **端口监听检查不得只依赖 `ss`**。ss 属于 iproute2,Alpine 这类最小镜像不装,
  而 busybox 自带 netstat。只用 ss 会让那种节点每次健康检查都判定"端口未监听",
  部署永远失败并回滚 —— 而服务其实是好的;
* **拨测打的是 sshd,而现代 OpenSSH 会因此封掉拨测的来源 IP。**
  OpenSSH 从 9.8 起默认开 `PerSourcePenalties`,其中 `noauth` 惩罚的正是
  「连上但没尝试认证就断开」—— 而拨测干的就是这件事(连 SSH、读一行横幅、
  关掉)。惩罚累积,一旦触发最少封 `min`(默认 15)秒、最多 `max`(默认 600)秒。
  于是每部署一次就攒一次,反复部署后拨测开始**间歇性**失败,而失败的表现是
  「连上了但读不到任何数据」,与「链路不通」一模一样。
  **真机实测**(Alpine + OpenSSH_10.2 的 NAT 中转机):开着惩罚时 25 次失败 5 次、
  15 次失败 6 次;临时 `PerSourcePenalties no` 之后 20 次零失败。
  更糟的是 `persourcenetblocksize` 默认按 /32 计,而 NAT 服务商上**一整片机器
  共用一个出口 IP** —— 惩罚会连坐到同一家服务商的其他机器,包括面板自己的
  管理连接。

  **根治办法是让拨测真的完成一次认证**(`sshx.AuthOverConn`):用面板自己的
  密钥在那条隧道上做一次完整的 SSH 公钥认证,而不是读一行横幅就断开。
  成功的认证不在任何一档惩罚里,所以拨测不再自我阻塞;顺带把验证强度也提上去了
  —— 隧道要能承载一个完整的双向协议,而且**对端的主机密钥必须与库里固定的
  那把一致**,那一条直接回答了「这条隧道到底有没有到那台机器」,
  而读横幅永远回答不了。真机实测(jp-60 → jp-166 公网 SSH,同一条路径):
  读横幅那一版跑到第 22 次开始出现「连上后 5.0 秒 EOF」,认证那一版
  40 次零失败,而且**跑完之后紧接着的读横幅探测也没被拖累** —— 说明它确实
  没有攒下惩罚。认证之后立刻断开、不开任何会话:要的只是"认证通过"这个事实。

  另外两件事仍然要做:拨测失败时把目标 sshd 的这项设置查出来写进报错
  (`sshdPenaltyNote`,存量节点上仍会遇到已经攒下的惩罚);以及**拨测必须重试**
  (`dialWithRetry`),退避时长按目标 sshd 自己的 `min` 取值现算,不写死。
  **一次拨测就足以触发惩罚**,于是有三种必然踩中的情形:同机两个入站时
  前一个把后一个封掉;协调器防抖只有 4 秒,连着两次部署时后一次必失败;
  把出口指向同一台落地的第三个节点撞上前两个留下的惩罚 ——
  三种都是健康节点被判失败并回滚。`checkPortListening` 早就因为同样的理由
  改成了轮询,而拨测这一步一直是"一次定生死";

* **带回节点日志时只能带探测客户端自己那几条。** 探测客户端跑在节点本机、
  连的是 `127.0.0.1:<入站端口>`,按这一点过滤。不过滤的话,同一个入站上
  真实用户的报错会被当成这次拨测的原因带回去 —— **已经发生过**:
  一次拨测失败带回了三行【用户】的连接(时长 10m10s、7m57s),
  而那与拨测毫无关系,只会把排查引向完全错误的方向。
  宁可什么都不带,也不能带错的;

* **VLESS 拨测的目标端口必须取自节点上的 `$SSH_CONNECTION` 第四段,
  不能用节点记录里的 SSH 端口**。NAT 小鸡上后者是服务商映射的外部端口,
  节点本机的 127.0.0.1 上没有东西监听它,拨测一律读到 EOF ——
  看起来完全就是"VLESS 链路不通",健康节点因此被判失败并回滚;
* **节点 root 口令只用于把面板公钥装进节点的那一次连接,绝不落库、
  不写日志、不进审计详情**;面板已经持有节点 root 权限,再存一份口令
  只会放大爆炸半径。持久访问一律用面板专用密钥(`settings.KeyManager`,
  私钥主密钥加密存 `system_settings`);
* `nodes.ssh_key_encrypted` 为空串表示"该节点用面板专用密钥"。
  空私钥必须存空串而不是加密后的空串 —— 后者不为空,会被当成一把解不开的私钥;
* **引导装完公钥后必须用面板密钥真连一次做验证**。只写不验的话,
  sshd 的 `AuthorizedKeysFile` 指向别处、或家目录权限过宽被 sshd 忽略这类问题,
  要等到第一次部署才暴露,而那时管理员已经以为节点接好了;
* 节点资源采集走 `pool.Do`,与部署、流量同步共用节点级互斥锁,
  且每次采集要在节点上 sleep 一秒。采集间隔默认 5 分钟,不要为了"实时"调小;
* 运行期可改的设置(订阅站点根)存 `system_settings` 并优先于配置文件,
  读取一律经 `settings.Store`,不要直接读 `cfg.HTTP.BaseURL`;
* 订阅 Token 同时保存 SHA-256 哈希与主密钥加密的密文:
  哈希供公开订阅路由查找(该路径不解密任何字段),密文供面板反复展示订阅地址;
* **接口返回的数组字段一律不得是 nil。** Go 的 nil 切片序列化成 JSON `null`
  而不是 `[]`,而前端把这些字段当数组用(`problems.length`、
  `build_tags.join(',')`、`problems[0]`)。最难查的是它**只在成功路径上出现**:
  探测一切正常时 `ProbeResult.Problems` 恰恰是 nil,于是"探测成功"反而让
  详情抽屉在渲染期抛 TypeError —— 内容整个被卸载、遮罩却留在屏幕上,
  表现为"点一下探测,详情页就没了,屏幕一片灰";探测失败时 Problems 有内容,
  反倒能正常显示。`ProbeResult` 与 `DestCheckResult` 各自只能经
  `newProbeResult` / `newDestCheckResult` 构造,`jsonarrays_test.go` 盯着它们;

* 注释用中文,只写代码本身表达不了的约束与原因,不写"这行做什么"。

## 常用命令

```bash
make test          # 运行 Go 测试
make lint          # go vet + gofmt
make web           # 构建前端到 web/dist
make build         # 构建二进制(需先 make web)
make build-linux   # 交叉编译 linux amd64/arm64
make run           # 本地启动后端
make dev           # 启动前端开发服务器(API 代理到 127.0.0.1:8080)

go run ./cmd/litebox genkey    # 生成主密钥
go run ./cmd/litebox migrate   # 只执行迁移并做一致性检查
go run ./cmd/litebox ssh-key   # 打印面板专用的节点访问公钥

bash scripts/build-singbox.sh  # 构建带 with_v2ray_api 的节点二进制到 assets/singbox
bash scripts/fetch-mieru.sh    # 下载并校验 mita/mieru 二进制到 assets/mieru(版本写死在脚本里)
```

针对真实节点的集成测试(会重启节点服务,不要指向生产节点):

```bash
export LITEBOX_TEST_NODE_HOST=1.2.3.4
export LITEBOX_TEST_NODE_PORT=22
export LITEBOX_TEST_NODE_KEY=~/.ssh/id_ed25519
go test ./internal/deployment/ -run TestIntegration -v -timeout 15m
```

## 已知依赖问题

`modernc.org/sqlite` 锁定在 v1.39.1。更高版本依赖的 `modernc.org/libc v1.74.1`
在模块代理上的 zip 损坏,升级前需先确认可正常下载。

## 阶段状态

* Phase 0 技术验证 —— 已完成,见 `docs/开发计划/v1/Phase0-技术验证报告.md`
* Phase 1 项目骨架 —— 已完成(配置、加密、迁移、登录会话、审计、REST API、Vue 骨架、部署文件)
* Phase 2 节点能力 —— 已完成(SSH 连接池、节点探测、握手目标检测、配置渲染、部署事务、三步健康检查、自动回滚)
* Phase 3 用户与配置生成 —— 已完成(用户 CRUD、节点分配、合并部署协调器、配置 diff)
* Phase 4 流量与额度 —— 已完成(Stats gRPC 客户端、SSH 通道采集、重启判定、ledger 入账、额度检查、定时调度)
* Phase 5 订阅 —— 已完成(VLESS URI、sing-box 客户端配置、节点过滤、公开路由、访问记录)
* Phase 6 管理页面 —— 已完成(仪表盘、用户管理、节点管理、部署记录、审计日志、流量图表)
* Phase 7 部署与加固 —— 已完成(安装升级备份恢复脚本、日志轮转、故障演练、128MB 压测)

V2(见 `docs/开发计划/v1_1/v2阶段开发计划.md`):

* Phase 8A 数据模型与访问等级 —— 已完成(access_tiers、有效节点视图、统一解析器)
* Phase 8B 节点展示名称 —— 已完成(display_name、排序、订阅开关、公开备注、维护说明)
* Phase 8C 用户认证 —— 已完成(portal_accounts、独立会话、限流、改密与重置)
* Phase 8D 用户门户 —— 已完成(概览、订阅、节点、流量、安全设置)
* Phase 8E 节点监控页面 —— 已完成(仪表盘卡片、数据过期提示、6/24/72/168 小时趋势)
* Phase 8F 续期与管理增强 —— 已完成(调整记录、筛选、批量操作、预警)

**V2 全部验收标准通过**,见 `docs/开发计划/v1_1/V2-验收报告.md`。

V2.1(见 `docs/开发计划/V2/v2.1版本开发计划.md`):

* Phase 9A IPv6 数据模型 —— 已完成(迁移 0011、地址校验与标准化、审计)
* Phase 9B IPv6 订阅 —— 已完成(逻辑展开、三种格式、排序与权限过滤)
* Phase 9C 节点周期流量 —— 已完成(周期边界、ledger 汇总、批量接口、告警等级)
* Phase 9D 管理页面 —— 已完成(表单、列表进度条、详情周期卡片)

**V2.1 全部验收标准通过**,见 `docs/开发计划/V2/V2.1-验收报告.md`。

V3 UI 改造(设计规范见 `docs/开发计划/v3/litebox-web-ui/`,该目录已被 .gitignore):

* Phase 10A Token 层与公共组件 —— 已完成(tokens/antd 主题、13 个 Lb 组件、自托管字体)
* Phase 10B 后端配套字段 —— 已完成(节点 `config_state`/`needs_deploy`、
  全站每日流量接口、用户 `next_reset_at`、每日序列不再补 0)
* Phase 10C 管理后台六页 —— 已完成(布局、仪表盘、用户、节点、部署记录、审计、设置、登录)
* Phase 10D 用户门户六页 —— 已完成(登录、概览、订阅、节点、流量、安全设置)
* Phase 10E 窄屏与收尾 —— 已完成(<768 整表换卡片、旧组件清理、文档)

V4(见 `docs/开发计划/v4/v4版本开发计划.md`,分两大块):

**第一块 节点落地协议** —— 已完成:

* Phase 11A 数据模型与配置渲染 —— 已完成(迁移 0014、协议分派、两套用户凭据、
  逐字节兼容测试)
* Phase 11B 部署、订阅与页面 —— 已完成(时钟偏移第零步、按协议拨测、
  `deployed_protocol` 驱动订阅、SIP002 `ss://`、表单与详情)

阻塞项(SS 的用户级流量是否进 V2Ray Stats)已在真机验证通过,
连同时钟盲区的实证见 `docs/开发计划/v4/V4-技术验证报告.md`。

**第二块 外部代理管理** —— 已完成:

* Phase 11C 数据模型与手工新增 —— 已完成(迁移 0015 三表一视图、externalproxy 包、
  ss:// 两种方言解析、粘贴链接自动填表、额外授权)
* Phase 11D 订阅合并 —— 已完成(分组排序、raw_uri 透传、两级到期过滤、
  `subscription_external_position` 设置)
* Phase 11E 导入与同步 —— 已完成(HTTP 拉取限制、格式识别、三步向导、
  公告条目识别、identity_key 两级匹配、字段锁定、消失策略、自动同步调度)
* Phase 11F 管理页面与门户 —— 已完成(源卡片 + 条目表格 + 窄屏卡片、
  危险操作分档、门户「其他线路」段、侧栏改「自建节点 / 外部代理」)

界面上「节点管理」改称「自建节点」,**路由、API、表名、审计 target_type
一个字没改** —— 重命名一个被十几处引用的标识,收益只是名字好看一点,
而审计日志里已经写死的历史记录改不回来。

V5 配置文件订阅(见 `docs/开发计划/v5/v5版本开发计划.md`):

* Phase 12A 渲染引擎与数据模型 —— 已完成(迁移 0016、占位符解析与校验、
  注释感知、tag 统一分配、落地判定与 detour、JSONC/YAML 语法自检、Store CRUD)
* Phase 12B 订阅链接与门户 —— 已完成(`/sub/{token}/profile/{id}[/{filename}]`、
  门户试渲染与不可用原因、内置 sing-box 去 block、tag 不再改写展示名)
* Phase 12C 管理页面 —— 已完成(订阅配置页、编辑抽屉、占位符速查与插入、
  上传、按真实用户预览、窄屏卡片)

示例配置在 `docs/开发计划/v5/` 下(该目录在 .gitignore 里),
其中 `singbox配置示例/config.模板.json` 是**已改好占位符、可直接上传**的那一份。
`internal/subscription/manualverify_test.go` 对着这三份真实配置跑完整流程,
默认跳过,设 `LITEBOX_V5_SAMPLES` 指向该目录才运行。

V6 节点 TCP 调优(见 `docs/开发计划/v6/v6版本开发计划.md`):

* Phase 13 一键 TCP 调优 —— 已完成(事实采集、按内存现算方案、直写 `/proc/sys`
  加读回验证、`/etc/sysctl.d` 持久化与开机加载确认、原值快照与一键还原、
  冲突文件检测、节点详情里的检查/应用/还原面板)
* Phase 14 入站监听选项 —— 已完成(迁移 0017、节点级 TFO 开关与
  `deployed_tcp_fast_open`、`udp_timeout` 按内存分档、diff 白名单同步、
  订阅只下发已生效值、表单开关与详情双行)

在一台 457MB 的 Debian 12 真机上跑完了检查 → 应用 → 还原的完整闭环,
见 `docs/开发计划/v6/V6-验收报告.md`。**`fs.file-max` 的"只提高不降低"规则
正是那次实测发现的** —— 按内存算出来的值比 systemd 已经设好的低三个数量级。

V7 节点中转(见 `docs/开发计划/v7/v7版本开发计划.md`):

* Phase 15 技术验证 —— 已完成,见 `docs/开发计划/v7/V7-技术验证报告.md`
  (落地 = Debian 12 / systemd,中转 = Alpine 3.23 / OpenRC / **NAT**)。
  十项全部通过,其中三项推翻了原计划的写法:`route.final` 缺失是静默的、
  stream 模块在两个发行版上都不随 nginx 一起装、OpenRC 默认没有 reload
* Phase 16A 数据模型与可见性 —— 已完成(迁移 0018、`nodes.role`、链式六列、
  `node_relays`、`user_effective_relays` 视图、`chain_code` 计数器、
  `DirtyKind` 双通道、跨节点脏标记传播)
* Phase 16B Nginx 透传 —— 已完成(nginx/stream 探测与安装、独立实例的
  systemd 与 OpenRC 服务定义、配置渲染、部署事务与四步健康检查、回滚、
  卸载覆盖 `litebox-nginx`)
* Phase 16C sing-box 链式 —— 已完成(`Config.Route` 与代理出站、
  `AssertChainRouted` + `TestChainImpliesRouteFinal`、链路凭据、
  两阶段有序部署、`compareNodeAttrs` 白名单同步)
* Phase 16D 订阅与门户 —— 已完成(`PhysicalRelay` 与 IPv6 展开、
  复用既有条目转换、authority 替换、门户第三组)
* Phase 16E 管理页面 —— 已完成(角色列与创建时选角色、节点详情的「转发」面板、
  转发规则增删改、nginx 现状、出口去向、危险操作分档)

**V7 全部验收标准通过**,见 `docs/开发计划/v7/V7-验收报告.md`
(面板驱动、两台真机:Debian 12/systemd 落地 + Alpine/OpenRC/NAT 中转)。
两种模式的真实客户端都验证了出口是落地 IP;流量口径实测为
「两台机器各记一份、用户只扣一次、全站合计不重复」。

**中转机是一个 128MB 的 LXC 容器**,验收中量出 `free -m` 报宿主机的 962MB
而 `/proc/meminfo` 是容器视角的 122MB —— 面板读的是后者,也就是对的那个。
按 962 去算会得到 7696 个 worker_connections 与 5 分钟的 UDP 驻留,
在 128MB 的容器上那是 OOM 的直接路径。

V8 sing-box 多入站(约束见上面「sing-box 多入站约束(V8)」):

* Phase 17 数据模型与渲染 —— 已完成(迁移 0019、`node_inbounds`、
  `user_effective_inbounds`、入站 tag 存库、`route.rules` 按入站分流、
  `AssertChainRouted` 加强成逐入站双向断言)
* Phase 18 部署、订阅与门户 —— 已完成(逐入站的端口监听与真实拨测、
  逐入站的拨测目标、`MarkDeployed` 清理离场入站、订阅与门户按入口出条目、
  中转与链式的落地改指入站)
* Phase 19 管理页面 —— 已完成(「入口」Tab 变成 sing-box 入站的管理列表、
  逐入口的编辑/握手目标/出口/删除、建节点表单只留第一个入口)
* Phase 20 等级降到入口 + 入口页合并 —— 已完成(迁移 0020:机器不再有访问等级、
  额外授权穿透入口等级、`user_effective_nodes` 改为入口层的投影;
  两类入口合并成一张表并保留类型标记;操作按钮按类型分两行;
  「部署」与「安装 sing-box」移进入口 Tab)

**V8 全部验收标准通过**(面板驱动、两台真机:Debian 12/systemd + Alpine/OpenRC/NAT)。
决定性的一条实测:**同一台机器上两个入口,真实客户端量到两个不同的出口 IP**
—— VLESS 入口(24443)出口是本机 23.169.169.110,Shadowsocks 入口(28443)
链到另一台后出口是 154.31.157.27。流量口径实测:两次 5MB 下载,
用户合计 +10,031,857、入口机 +10,031,857、落地机 +5,015,893、
全站每日 10,045,273(等于用户合计,链路凭据不重复计)。

V9 外部代理多协议(约束见上面「外部代理约束(V4,协议在 V9 扩展)」):

* Phase 21 多协议外部代理 —— 已完成(vmess/vless/trojan/hysteria2/tuic 的
  分享链接解析、`Params` 扩展、`externalproxy.SingBoxOutbound` 单一实现、
  `singbox.Outbound` 补齐外部协议字段并保持自建节点渲染逐字节不变、
  `ChainOutbound.Prebuilt`、按能力过滤出口与转发落地、
  `ParseOutboundSSMethod` 修掉「登记得进来、当出口时部署失败」那个分叉)

**没有迁移。** `external_proxies.protocol` 本来就没有 CHECK 约束,
`params_encrypted` 本来就是一份 JSON —— 加字段向后兼容,存量记录一行不动。

V10 巡检、推送与配置不落盘(约束见上面「服务巡检、推送与配置不落盘约束(V10)」):

* Phase 22 消息推送 —— 已完成(`internal/notify`:Bark 与 Telegram 两个渠道、
  事件类型与冷却、有界队列、凭据脱敏与跨主机重定向拦截、设置页与同步测试)
* Phase 23 服务巡检与自动恢复 —— 已完成(`node.Watchdog`:逐节点问
  sing-box 与 nginx、UNREACHABLE 与 STOPPED 分开、先拉起后重新下发、
  拉起后复验、退避、`systemctl reset-failed`、节点列表与详情的巡检列)
* Phase 24 配置不落盘 —— 已完成(迁移 0021、`Layout.ConfigInRAM` 与
  `ConfigPath()`/`ConfigBackupDir()`、实测 /run、有序切换与失败回退、
  卸载清理 RuntimeDir)

V11 入口级 IPv6 条目(约束见上面「入口级 IPv6 条目约束(V11)」):

* Phase 25 IPv6 条目开关与独立名称 —— 已完成(迁移 0022、
  `subscription.IPv6EntryName` 单一回落、`PhysicalNode.IPv6Enabled/IPv6Name`、
  派生字段 `ipv6_entry_name`、门户 `supports_ipv6` 跟随入口开关、
  入口表单的 IPv6 区块与改名确认、入口列表的地址族标签)

**没有推翻任何既有行为**:两列的默认值让存量数据的订阅输出逐字节不变,
只有管理员显式动手才会出现「用户客户端里多一份节点」那个代价。

V12 订阅 IPv4 地址(约束见上面「订阅 IPv4 地址约束(V12)」):

* Phase 26 管理地址与订阅地址分开 —— 已完成(迁移 0023、
  `subscription.SubscriptionIPv4` 单一回落、`PhysicalNode/PhysicalRelay`
  的展开、派生字段 `subscription_host`、`chainInboundTarget` 的落地地址、
  节点表单与详情/列表的三栏地址)

* Phase 27 Clash / mihomo 原生订阅 —— 已完成(`?format=clash`、
  `Entry.Proxy` 与独立名字空间、`externalproxy.ClashProxy` 与协议覆盖 parity 测试、
  V5 模板的 `$(clash_proxies)` 等四个占位符、`checkNoHardcodedProvider`、
  门户订阅页第二条链接)

**端口一列都没加** —— 三层端口在迁移 0019 就建好了。
存量节点这一栏为空,订阅输出与升级前逐字节相同。
Clash 输出是**新增**的第四种格式,原有三种一个字节都没变。

V13 Mieru 落地协议(约束见上面「Mieru 落地协议约束(V13)」):

* Phase 28 数据模型、端口段与订阅 —— 已完成(迁移 0024 第三类入口表 +
  `proxy_users.mieru_password_encrypted` + `user_effective_mieru_inbounds`、
  `internal/mieru` 的取值与 `PortRange`、`internal/nodeport` 把三处端口
  冲突检测统一成一份、`mierus://` 与 mihomo proxy、管理接口与入口页的第三种类型)
* Phase 29 出口落地 —— 已完成(迁移 0025、每个入口一个 mita 实例、
  `mieru-egress-` 回环 socks 入站 + `route.rules` 按入站分流、
  链路凭据 `chain_xxxxxx` 与 `checkMieruChainTargetReady`、
  三步有序下发)。**真机验证推翻了原来「Mieru 不能链式」那一条** ——
  不能的只是"当落地",它自己完全可以有出口
* Phase 30 部署事务 —— 已完成(`litebox-mita-<入口id>` 逐实例的服务定义、
  `unshare --mount` 私有 metrics 目录、apply/reload/stop-start 三种下发路径、
  四步健康检查含真实拨测与 SSH 公钥认证、失败回滚)
* Phase 31 流量采集 —— 已完成(迁移 0026 给基线加 `source` 维度、
  `internal/mieruapi` 经 direct-streamlocal 读管理 gRPC、
  每实例一个 `batch_id`、重启前强制同步)
* Phase 32 管理页面 —— 已完成(入口页第三种类型、Mieru 单独一行工具条、
  逐行的下发与出口按钮、`MieruChainModal` 把三步下发的顺序与后果写进确认框)
* Phase 33 全面接入 —— 已完成(迁移 0027/0028;探测、巡检、配置比对、
  概览与入口管理五处的判据都改成"这台机器上有几类入口";
  按服务的装/卸接口与「入口」Tab 里的九个动作;`NodeOpProgressModal`;
  顶栏菜单只留机器级操作;「扫描握手目标」搬进 sing-box 入口表单)

真机验证在 101-la(Debian / systemd / 公网)与 lax-1(Alpine / OpenRC / NAT)
上跑完,决定性的一条:**同一台机器上三个 Mieru 入口,真实客户端量到三个
不同的出口 IP**。流量口径实测:每实例基线各约 10,514,2xx,
落地侧 `chain_000001` 记 10,514,259,入口机 `user_000001` 记 31,548,723,
用户合计 31,600,321 字节 —— 三份计数器各自入账、用户只扣一次。

Phase 33 的几个修复都是生产上撞出来的,不是照着代码推的:
只有 Mieru 入口的机器对谁都是空的(视图借道了只认 sing-box 的那一层)、
Mieru 的下发记录一条都存不进去(CHECK 漏了一种)、装过一次就再也装不上
(覆盖正在被执行的二进制)、以及探测把「没装 sing-box」判成节点离线。
四个的共同点是**都不报错、或者报一个指向错误方向的错**。

未完成当前阶段前,不要提前开发后续阶段的功能。
